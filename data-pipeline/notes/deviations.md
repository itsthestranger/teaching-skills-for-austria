# RIS Data Deviations Log

This file records every divergence between the written specification for competence and curriculum data and what is actually found in the official RIS (Rechtsinformationssystem des Bundes) source materials. 

When data is ingested or a mismatch is discovered, the resolution (adapted spec, manual correction, note for users, etc.) is documented here to maintain traceability and justify decisions made during data processing.

**Reading the Resolution column.** A row marked `DECIDED + DONE` describes shipped behaviour. A row that opens with `Decision:` or `Decision <id> (taken <date>)` records a decision whose implementation is tracked as a backlog task and **may not be in the code yet** — take the row as binding intent, not as a description of current behaviour, and check the referenced task before assuming. A row never silently replaces an earlier one: a superseding row says so and the original stays.

| Date | Source | Expected | Actual | Resolution |
|------|--------|----------|--------|------------|
| 2026-07-31 | Both live RIS curriculum documents, every emitted `Anwendungsitem` (E12-07 / V-65) | Theme-marker handling on application items should match the established `Kompetenz` contract | Measured application-item totals / items with markers: SEK1.M **237 / 21**, SEK1.D **54 / 15**, PRIM.D **37 / 11**, PRIM.SU **40 / 1**; SEK1.E and PRIM.M emit no application items. Before E12-07 the markers remained visible only in `text_roh`; after applying the trailing legend, all **48 / 48** marked items resolve and **0** retain an unresolved footnote. The existing item core fields (`id`, `text`, `text_roh`, linkage and metadata) are unchanged. | **DECIDED + DONE.** `Anwendungsitem` now carries `uebergreifende_themen`, `themen_marker_roh` and `fussnoten_unaufgeloest`. Both item emitters resolve through the same `_resolve_super` path as competences, and `_finish` re-resolves items after the trailing theme legend has been parsed. Parser serialization always contains all three arrays; tests freeze both emitter paths, raw-marker preservation, live counts and late-legend resolution. |
| 2026-07-31 | SEK1.M shipped shard serialization after E12-07 | The three application-item theme arrays could be emitted unconditionally, mirroring the in-memory dataclass | Unconditional empty arrays grew the five payload parts from **177,571** to **208,959 bytes**. Emitting the full trio only for marked items reduced that to **182,679 bytes**, but left `zahlen.json` at **50,168 bytes**, above its soft target. Emitting each optional array only when non-empty yields **181,755 bytes** across payload parts; largest part `zahlen.json` is **49,904 bytes**, and validation reports no soft finding. Exactly **21** of 237 items gain theme data; after removing those new keys, every payload document is identical to the previous shipment. `index.json` differs only in byte/token metadata. | **DECIDED + DONE.** Build output uses sparse, per-field serialization: omission means that array is empty, as documented by the optional schema properties. This preserves unresolved-only future cases while avoiding redundant empty arrays. Rejected: unconditional arrays (material payload bloat) and an atomic three-field bundle (soft-size regression). Shipped SEK1.M and its checksum manifest were regenerated and audited. |
| 2026-07-27 | live RIS discovery, GN 20007850 (Mittelschule), NOR40271471 | `Data.Dokumentliste.ContentReference` is a single object (per validated API facts) | `ContentReference` is a **list**: one `ContentType: "MainDocument"` entry plus several `ContentType: "EmbeddedAttachment"` entries (inline PNG images referenced by the curriculum text) | `fetch_ris_resources.py::get_content_urls` normalises `ContentReference` to a list (same treatment as the documented `ContentUrl` quirk) and selects the entry with `ContentType == "MainDocument"` before reading its `Urls.ContentUrl`. Embedded attachments are not downloaded by this pipeline. |
| 2026-07-27 | live RIS discovery, GN 20006166 (Bildungsstandards), NOR40255561 | `Kurztitel` = "Bildungsstandards-Verordnung" (per task's regulation table) | Live `Data.Metadaten.Bundesrecht.Kurztitel` = "Bildungsstandardsverordnung" (no hyphen, single word) | Not a functional issue -- `fetch_ris_resources.py` records the live `Kurztitel` verbatim in the manifest (source wins); the hyphenated spelling was only ever a task-doc convenience label. |
| 2026-07-27 | NOR40271469 (Volksschule) + NOR40271471 (Mittelschule), all `table` elements | Structural signal table lists `absatz/@typ="erltext"` as **the** class-year / school-year marker | True for Sek I (57 of 61 markers) but **false for primary**: in the VS document 35 of 40 markers are `ueberschrift/@typ="erll"` (e.g. `1. Schulstufe:` at child 1237), only 5 are `erltext` | `parse_lehrplan.py::LehrplanParser._classify` recognises a level marker from **either** element type whenever the text matches `^\d+\.\s*(Klasse\|Schulstufe)\s*:?$`. Documented in `notes/ris-xml-structure.md` §4. Consequence for extension: in primary a level marker and an area heading share an element type, so the order of checks in `_classify` is load-bearing. |
| 2026-07-27 | NOR40271471, Sek I MATHEMATIK, Anwendungsbereiche section | 237 application-area items are all precisifications of a competence (validated count read as "items per competence") | 237 is the **section total**. Only **198** precisify a competence; the other **39** are the four per-class-year `Vorschläge für den Einsatz digitaler Technologien` lists, which follow no competence sentence and precisify nothing. None of the 39 contains `allenfalls` (all 32 `allenfalls` items are precisifications) | Count preserved: the parser emits all 237 as application items, discriminated by `art` (`praezisierung` \| `digitale_technologien`). The 39 carry `kompetenz_id: null` and an ID segment `DT.` instead of `AB.`. Downstream must filter on `art` before treating an item as a Lehrstoff precisification. |
| 2026-07-27 | NOR40271471, Sek I MATHEMATIK, children 905/968 and 918/998 | Application areas link to competences by **verbatim** text repetition | 38 of 40 repetitions are verbatim; **2 are not.** (a) child 968 drops "von Rechtecken" from the competence sentence at 905; (b) child 998 writes "achsensymmetrische Figuren **und** zueinander kongruente Figuren" where 918 has "**sowie**" | Join is a 3-stage cascade (exact → fuzzy ≥ 0.90 → positional by area+year+ordinal). Measured: 38 exact (95.0 %), 2 fuzzy (5.0 %), 0 positional, 0 unmatched. Both non-exact joins are logged as `join_fuzzy` issues. Threshold justified in `notes/ris-xml-structure.md` §5; re-measure per subject before reuse. |
| 2026-07-27 | NOR40271471, Sek I MATHEMATIK, children 1063–1069 | Sek I Mathematik has 40 competence descriptions (4 areas × 4 class years) | 40 is correct for the four competence areas, but the subject carries **2 further** competence `listelem`s under a separate `erll` heading, `Kompetenzen für den Mathematik-Lehrplan bei integrativer Führung von Geometrisches Zeichnen (1. bis 4. Klasse):` (one for K3, one for K4). They belong to no `Kompetenzbereich` | Originally (see FINDINGS.md V-57): heading unrecognised, state machine leaves the section, logs `unbekannte_ueberschrift`, items carried in `ParseResult.zusatzbloecke`, not counted among the 40. **Superseded below (same day) -- promoted into the main dataset.** |
| 2026-07-27 | NOR40271471, Sek I MATHEMATIK, application items (25 affected) | `itertext()` yields the published sentence | Fractions and formulae ship as inline PNGs: `<binary><src>/Dokumente/Bundesnormen/NOR40271471/hauptdokument.img1is.png</src></binary>`. `itertext()` splices the **file path** into the middle of the sentence (e.g. "…in einfachen Fällen (zB 0,6/Dokumente/Bundesnormen/…img1is.png…") | Originally (see FINDINGS.md V-53): replaced by `[Abbildung]` with the path kept in `abbildungen` and the record flagged incomplete. **Superseded below (same day) -- images fetched and shipped, quotation now complete.** |
| 2026-07-27 | NOR40271471, per-subject footnote legend table (child 1071) | Cell text reads `4Informatische Bildung` | The footnote number is a `<super>` child (`<super>4</super>Informatische Bildung`). Any extractor that strips `<super>` -- which the competence path must, to keep the sentence quotable -- reads back `Informatische Bildung` and produces an empty number → theme map | `_parse_themen_tabelle` reads `ExtractedText.roh` (superscripts inlined) rather than the cleaned text. Same trap applies to the per-subject theme sentence at child 894. |
| 2026-07-27 | FINDINGS.md V-53. NOR40271471, all 64 `<binary>` images (63 inside the Sek I Mathematik span, 1 inside the following GEOMETRISCHES ZEICHNEN subject at child 1370, out of parser scope) | Formulae shipped as PNG have no textual representation; a flagged-incomplete quotation (`[Abbildung]` placeholder, path recorded but not fetched) was judged sufficient pending a decision | **Decision taken: fetch and ship the images.** They are inline glyphs, not diagrams (all exactly 17px tall, widths 4-227px measured -- wider range than the initially assumed 5-154px, see next row), ~40KB total for all 64, and are part of the regulation text (*freie Werke* under §7 UrhG, same as the words). `fetch_ris_resources.py` now discovers `<binary>/<src>` references in the fetched XML and downloads them with the same rate-limited/backoff HTTP client, into `resources/<key>/images/<nor>/` (gitignored). `abbildungen.py` (new, stdlib-only) installs them into `plugin/data/abbildungen/<nor>/` (shipped, committed) and reads PNG width/height from the IHDR chunk via `struct`. `parse_lehrplan.py::element_text` now emits the token `⟦ABB:<dateiname>⟧` (U+27E6/U+27E7, cannot occur in source text) at the exact image position instead of `[Abbildung]`, and every Kompetenz/Anwendungsitem whose text carries a token gets a matching `abbildungen` array entry (token, datei, nor, pfad, quelle_url, breite_px, hoehe_px, sha256) resolved against the shipped registry. Records with images are **no longer flagged as incomplete quotations**, because they no longer are. |
| 2026-07-27 | FINDINGS.md V-53 (continued). Measured facts vs. actual PNG dimensions/sizes, all 64 shipped images | Task brief's stated "measured facts": widths 5-154px, ~566 bytes average, ~35KB total | Actual (read via `abbildungen.py::read_png_dimensions` + file size): widths **4-227px** (min at `img23is.png`/`img24is.png` = 4px, max at `img34is.png` = 227px), heights uniformly **17px** (confirmed exactly as stated), average **617.4 bytes**, total **39516 bytes** (~38.6KB). Not a functional issue -- still small enough to ship without concern -- but the brief's numeric ranges were an approximation, not an exact measurement; this row records the exact ones. |
| 2026-07-27 | FINDINGS.md V-57. NOR40271471, Sek I MATHEMATIK, children 1063-1069 (the "Kompetenzen für den Mathematik-Lehrplan bei integrativer Führung von Geometrisches Zeichnen (1. bis 4. Klasse):" appendix, previously carried in `zusatzbloecke` -- see the row above) | 40 competence descriptions is the complete Sek I Mathematik set; whether the 2 extra belong in the shipped dataset was left open | **Decision taken: promote the 2 GZ-integrative competences (1 for K3, 1 for K4) into the main dataset.** `ERWARTET_SEK1_M["kompetenzen"]` moves **40 -> 42**. They fit none of the four official numbered Kompetenzbereiche (Zahlen und Maße / Variablen und Funktionen / Figuren und Körper / Daten und Zufall) -- the heading is structurally a fifth, separate list, not a fifth Kompetenzbereich. Assigned a synthetic, clearly-labelled area instead: `bereich_nummer=None`, `bereich_name="Integrative Führung von Geometrisches Zeichnen"`, ID slug `GZINTEGRATIV` (IDs `AT.LP23.SEK1.M.GZINTEGRATIV.K3.01` / `.K4.01`). That synthetic area is **not** added as a 5th entry to `ParseResult.bereiche` -- `kompetenzbereiche` stays 4, faithful to the regulation's actual structure; the area label lives only on the two Kompetenz records. Consequence: these two competences have no Anwendungsbereiche counterpart in the source at all (that section has no matching appendix), so `join_stats["kompetenzen_ohne_block"]` is now 2, not 0 -- expected and logged (`kompetenz_ohne_anwendungsblock`), not a join failure. The parser recognises the heading via `GZ_INTEGRATIV_RE` and a dedicated `KOMPETENZ_GZ_INTEGRATIV` state (mirrors `KOMPETENZBEREICHE` but with a fixed area); the per-subject footnote legend table that immediately follows is still parsed correctly from within that state. |
| 2026-07-28 | NOR40271471 (Mittelschule), DEUTSCH (children 395-563) and (ERSTE) LEBENDE FREMDSPRACHE (children 617-725), read while freezing the ID scheme (E3-02) | FINDINGS V-40 open question: "Do Sek I Deutsch and (Erste) Lebende Fremdsprache use the same section shape as maths, or a combined heading like primary?" | **V-40 closed: neither uses Mathematik's shape.** Both subjects use the primary-style single combined heading, `Kompetenzbeschreibungen und Anwendungsbereiche, Lehrstoff (1. bis 4. Klasse):`, with an `Anwendungsbereiche` sub-heading and its own `liste` inline after each competence area's list -- there is no separate top-level `Anwendungsbereiche (1. bis 4. Klasse):` section as in Sek I Mathematik. Not a parser change (out of scope for E3-02); recorded so the future `SEK1.D`/`SEK1.E` `SubjectSpec` work does not have to re-derive it. See `notes/id-schema.md` §4. |
| 2026-07-28 | NOR40271471 (Mittelschule), DEUTSCH, children 434-438, read while freezing the ID scheme (E3-02) | Every competence area either has its own competence list (`Kompetenzbereich <Name>` + `Die Schülerinnen und Schüler können` + `liste`), mirroring Sek I Mathematik's four numbered areas | Sek I Deutsch carries a fourth-in-sequence heading, `Integrativer Kompetenzbereich Sprachbewusstsein und Sprachreflexion`, that has **no competence list of its own** -- the source states explicitly its competences "werden in den Bereichen Zuhören und Sprechen, Lesen, Schreiben integrativ formuliert" -- but it **does** carry its own `Anwendungsbereiche` heading and item `liste` (child 437-438). This is the structural mirror of the already-known `GZINTEGRATIV` case (competences with no Anwendungsbereiche block); here an Anwendungsbereiche block exists with no competences of its own. Given a frozen ID-scheme area code, `SPRACHREFLEXION` (`data-pipeline/schema/id_schema.py`, `AREA_CODES["SEK1.D"]`), so a future `AT.LP23.SEK1.D.AB.SPRACHREFLEXION.K1.NN` item ID is representable; not implemented in `parse_lehrplan.py` (out of scope). |
| 2026-07-28 | NOR40271471 (Mittelschule), DEUTSCH g1 span, children 515-560, found while reviewing the E3-02 ID freeze | A subject's `ueberschrift/@typ="g1"` span contains exactly one curriculum, so scanning the whole span for `Kompetenzbereich` headings yields that subject's areas | The `DEUTSCH` g1 span (395-563) contains **two** curricula. After the main one ends (child 511), a second, complete curriculum follows at child 515: `LEHRPLANZUSATZ DEUTSCH ALS ZWEITSPRACHE FÜR ORDENTLICHE SCHÜLERINNEN UND SCHÜLER`. It is an `ueberschrift/@typ="erll"`, **not** a `g1`, so it does not open a new subject span and is invisible to `@typ="g1"` segmentation (**V-06**). It carries its own `Kompetenzmodell und Kompetenzbereiche` (531), its own `Kompetenzbeschreibungen und Anwendungsbereiche, Lehrstoff` (543) and **five further `Kompetenzbereich` headings**: Hören (545), Sprechen (548), Lesen (551), Schreiben (553), Linguistische Kompetenzen (556). Note this is a *different* document from the `LEHRPLANZUSATZ … FÜR AUSSERORDENTLICHE SCHÜLERINNEN UND SCHÜLER IM DEUTSCHFÖRDERKURS`, which *does* get its own `g1` at 563 and which the plan already says to skip. | **Hard-fail risk, not cosmetic.** `Lesen` and `Schreiben` occur in both the main Deutsch curriculum and the DaZ Lehrplanzusatz. A `SEK1.D` area extractor that scans the full g1 span therefore mints `LESEN`/`SCHREIBEN` twice under the same `Band.Fach`, producing duplicate IDs — the pipeline's only hard-fail condition (`ParseError` on ID collision). The frozen `AREA_CODES["SEK1.D"]` table deliberately contains **only the four main-curriculum areas** and excludes the five DaZ ones; the DaZ Lehrplanzusatz is out of v1 scope, consistent with the plan's treatment of the Deutschförderkurs. Whoever writes the `SEK1.D` `SubjectSpec` must bound the competence section at the `LEHRPLANZUSATZ` heading (child 515), not at the end of the g1 span. |
| 2026-07-28 | `resources/manifest.json`, `mittelschule.kundmachungsorgan`, read while building `data-pipeline/build_dataset.py` (E3-03/E3-04) | Plan section 4.9's provenance jsonc block gives `"kundmachung": "BGBl. II Nr. 185/2012 idF BGBl. II Nr. 178/2025"` (abbreviated `idF` form) | Live manifest value is `"BGBl. II Nr. 185/2012 zuletzt geändert durch BGBl. II Nr. 178/2025 "` — the RIS `Kundmachungsorgan` metadata field spells the amendment out in full, plus a trailing space (an artefact of manifest generation, not part of the citation). | `build_provenienz()` takes the live manifest value verbatim (source wins), stripped of only the trailing whitespace (not part of the citation's official wording); the abbreviated `idF` form in the plan's example was illustrative, not the field to reproduce literally. Same treatment for `volksschule.kundmachungsorgan`. |
| 2026-07-28 | E3-04 shard output, revisited after coordinator review of the E3-07 size report | Plan section 4.9: one file per subject×band at `plugin/data/kompetenzen/<band>/<fach>.json` | The single-file Sek I Mathematik shard measured ~65k approx. tokens (`plugin/data/kompetenzen/sek1/m.json`, since deleted) — unusable per plan section 5's B1 contract (`SKILL.md` loads the shard file **directly** into context), and far over section 6.7's ~15k token soft target. Measurement showed most of the bytes were structural duplication (identical `provenienz` per record, `text_roh` repeating `text` on 248/279 records, `band`/`fach`/`bereich_name`/`bereich_nummer` repeating what a record's position already implies), not irreducible verbatim regulation text (~33 KB / ~8k tokens). | Replaced the single-file output with plan section 6.7's own prescribed remedy ("split finer, e.g. per competence area"): `plugin/data/kompetenzen/<band>/<fach>/<bereich-slug-lowercase>.json` (one per Kompetenzbereich, each self-contained with a full `meta` block), plus `zusatz.json` (`zusatzkompetenzen[]` + `digitale_technologien_vorschlaege[]`, `kompetenzbereiche: []`) and `index.json` (discovery: `meta` + per-part filename/area/counts/size, so a skill can pick a part without loading it). Combined with the record slimming (omit `text_roh` when byte-identical to `text`; drop `band`/`fach`/`bereich_name`/`bereich_nummer` where a record's position already implies them), 3 of 5 Sek I Mathematik parts now pass the §6.7 target outright; `variablen.json` and `zahlen.json` still exceed the *byte* target (not the token target) solely because `abbildungen[]` entries could not be slimmed -- see the next row. This is a REVIEW trigger per E3-07, not a build failure. **Whoever writes the E4 access layer must read a directory (`<band>/<fach>/`), not a single `<fach>.json` file.** |
| 2026-07-28 | `schema/kompetenzen.schema.json` `$defs/abbildung` (frozen, not touched by this task), found while implementing the coordinator's abbildungen-slimming instruction | Coordinator instruction: move `nor`/`quelle_url`/`breite_px`/`hoehe_px`/`sha256` out of every inline `abbildungen[]` entry into a new `plugin/data/abbildungen/registry.json`, keeping only `token`/`datei`/`pfad` inline (verified as the only fields the renderers read at render time: `split_abbildungen`, `resolve_abbildung_path`, `abbildung_missing_marker`, `abbildung_alt` in `plugin/skills/at-unterrichtsplanung/scripts/lesson_common.py`) | `$defs/abbildung.required` in the frozen schema lists all eight fields, including the five the instruction asked to move out. A slimmed `abbildungen[]` entry (3 fields) fails schema validation; the schema is off-limits for this task. | Kept `abbildungen[]` entries at their full eight fields in every record (no bytes saved there) so every part still validates against the frozen schema; built `plugin/data/abbildungen/registry.json` anyway as an additive, standalone index of the same five fields (useful on its own, e.g. an asset-integrity check that doesn't want to load a shard) rather than as a replacement. This is the reason `variablen.json`/`zahlen.json` still exceed the §6.7 byte target after the split (row above) — flagged as a REVIEW, not a failure, and reported back rather than silently resolved by editing the schema. **Superseded below (same day) -- the schema restriction was lifted by the coordinator on the strength of this row's own renderer-audit evidence, and `abbildungen[]` is now genuinely slimmed.** |
| 2026-07-28 | Coordinator follow-up to the row above, same task | `schema/kompetenzen.schema.json` stays off-limits; `abbildungen[]` stays at full width; `nor`/`quelle_url`/`breite_px`/`hoehe_px`/`sha256` ship twice (inline and in `registry.json`), which is the direct cause of `zahlen.json`/`variablen.json` exceeding the §6.7 byte target | Coordinator lifted the schema restriction: "the schema restriction was mine, and it was wrong. Your finding that the renderers only ever read `token`, `pfad` and `datei` is exactly the evidence needed to change it." `$defs/abbildung.required` relaxed to `["token", "datei", "pfad"]`; the other five properties kept as valid-but-optional (not deleted -- a record may still legitimately carry them). `build_dataset.py::_abbildungen_slim` now actually drops the five fields; `build_dataset.py::assert_abbildungen_registry_complete` runs before any record is slimmed and raises `BuildError` if any referenced `datei` is missing from the registry or missing any of the five fields -- a hard build failure, not a silent data-loss path. `schema/beispiel_kompetenzen.json`'s `abbildungen` entries slimmed to match (its verbatim text and every other field are unchanged; `test_kompetenzen_schema.py`'s byte-equality regression tests still pass untouched). Result: all 5 Sek I Mathematik parts now pass both the byte and token §6.7 targets (previously 3 of 5; `zahlen.json` was 48,654 bytes / ~12,163 tokens, `daten.json` was already smallest at 19,957 bytes / ~4,989 tokens -- see the build report for the full table). `plugin/data/abbildungen/registry.json` is now the single, non-duplicated home for an image's provenance and dimensions. |
| 2026-07-29 | NOR40271471 + NOR40271469, all five not-yet-parsed target subjects (SEK1.D, SEK1.E, PRIM.D, PRIM.M, PRIM.SU), measured directly against the checked-in XML while planning the E2/E3-08 breadth work | BACKLOG risk register: "The primary join has no measured basis — primary Deutsch/Sachunterricht merge competences and application areas under one heading; the Sek I text-join model may not apply. Treat the primary join as an unsolved problem." Notes §9.1 likewise expected competence descriptions and application items to sit in sibling `liste` elements needing a re-measured text join | **There is no join to solve outside Sek I Mathematik.** The competence sentence is *never* repeated as an `absatz` outside SEK1.M — the V-27 text-repetition mechanism is unique to that one subject. Everywhere else the application items attach by **XML containment** to a container that is explicitly present in the markup, under an `absatz/@typ="abs"` whose text is exactly `Anwendungsbereiche`. Measured attachment, authoritative counts: **SEK1.D** 4 areas / 41 competences / **16** AB blocks / 54 items — one block per *area × class year* (`bereich`); **SEK1.E** 4 / 37 / **0** lists but **4** AB headings followed only by prose (`prosa`, zero items); **PRIM.D** 4 / 40 / **4** blocks / 37 items — one block per *school year*, after the last area of that year (`stufe`); **PRIM.SU** 6 / 48 / **4** blocks / 40 items — same `stufe` shape, and its items are broad topic labels ("Ich und Du und Wir", "Jahr und Zeit"), not per-competence precisifications; **PRIM.M** 4 / 40 / **0** — no Anwendungsbereiche at all (`keine`, already anticipated by V-24). | The risk is **retired, not mitigated**: fuzzy/positional matching is not needed for any subject but SEK1.M, so there is no per-subject join rate to re-measure and no threshold to re-tune. `SubjectSpec` gains an explicit `anwendungsbereiche_bindung` axis with five values — `kompetenz` (SEK1.M, keeps the existing 3-stage cascade untouched), `bereich` (SEK1.D), `stufe` (PRIM.D, PRIM.SU), `prosa` (SEK1.E), `keine` (PRIM.M) — and the parser attaches items by container for the four non-`kompetenz` values. **Consequence for the data model: `anwendungsitem.kompetenz_id` is only meaningfully populated for SEK1.M.** For `bereich` it must carry `(bereich, stufe)` and for `stufe` only `(stufe)`; a per-competence link must **not** be synthesised where the regulation does not make one, or the product would assert a binding attribution the source does not contain. |
| 2026-07-29 | NOR40271471 child 514/515 and the equivalent position in all six target subjects, measured while planning the same work | Earlier row (2026-07-28) recorded the SEK1.D `LEHRPLANZUSATZ DEUTSCH ALS ZWEITSPRACHE` at child 515 as a hard-fail ID-collision hazard (duplicate `Lesen`/`Schreiben` areas) and proposed bounding the SEK1.D competence section at that child index | The boundary does not need a hardcoded index, and a `^LEHRPLANZUSATZ` heading test alone is a subject-specific patch. **Every one of the six target subjects ends its main curriculum with exactly one 18-cell footnote legend table**, and that table is the last content element before the next subject or insert: SEK1.M 1071, SEK1.D **514** (immediately before the LEHRPLANZUSATZ at 515), SEK1.E 724, PRIM.D 897, PRIM.M 1291, PRIM.SU 1418. It is already parsed by `_parse_themen_tabelle` as the authoritative theme map (V-56), so the parser reads it anyway. | Adopt the legend table as the **general subject terminator**: on `TABELLE` in a competence/application state, parse the theme map and then leave the subject. This bounds SEK1.D at 514 without a magic number, removes the duplicate-area ID collision structurally, and is derived from the source rather than from one subject's accident. Keep an `^LEHRPLANZUSATZ` `erll` test as a cheap belt-and-braces second terminator. The frozen `AREA_CODES["SEK1.D"]` table can stay as it is — it already lists only the four main-curriculum areas — but it is no longer the thing preventing the collision. |
| 2026-07-29 | Both documents, `allenfalls` occurrences and area-heading element types across all six target subjects, same measurement pass | Notes §9.8 / plan §4.7: `verbindlich: false` via the `allenfalls` marker is a general per-item mechanism; §3's element-type trap (an area heading appearing as `absatz/@typ="abs"`) is a general hazard the state machine must tolerate everywhere | **Both are SEK1.M-only.** (a) `allenfalls` occurs 24× in the Mittelschule document and 7× in the Volksschule document, but **zero** times inside the competence/application sections of SEK1.D, SEK1.E, PRIM.D, PRIM.M or PRIM.SU — every in-scope occurrence is inside the SEK1.M Anwendungsbereiche span (children 950–1001) or in general/non-target parts. (b) Area headings are `ueberschrift/@typ="erll"` in all five remaining subjects; the `absatz/@typ="abs"` form exists **only** in SEK1.M's separate Anwendungsbereiche section. Meanwhile `Kompetenzbereich` appears in ordinary `absatz` prose 11× inside the five spans (SEK1.E 8, SEK1.D 2, PRIM.SU 1, e.g. "In allen vier Kompetenzbereichen wird das Zielniveau A1/A2 angestrebt"). | `verbindlich` stays `true` for every item outside SEK1.M and the `allenfalls` scan is not run for the other subjects — do not reuse the flag blindly (as §9.8 warned) and do not report a 0/N "non-binding" split as if it were measured signal. The `absatz`-as-area-heading rule becomes **opt-in per `SubjectSpec`** (a `bereich_aus_absatz: bool`, true only for SEK1.M) rather than a state-guarded global: with it off, all 11 prose false positives are impossible by construction instead of being suppressed by the state guard. This is a strict tightening — SEK1.M's behaviour is unchanged. |
| 2026-07-29 | Design decision on top of the `anwendungsbereiche_bindung` row above, taken with the coordinator while planning E3-08 | Open question from the investigation: where do `bindung: "stufe"` application items (PRIM.D, PRIM.SU — attached to a whole school year, not to a competence or an area) live in a shard that is split one file per Kompetenzbereich? Proposed storing them once under a shared `meta` and having the E4 access layer join them | Coordinator chose repetition over single-storage, to guarantee no information is lost at point of use. Measured cost of repeating the full year-bound block in every area part file: **PRIM.D 5,764 bytes per file × 4 area files = 23,056 bytes**; **PRIM.SU 1,169 bytes per file × 6 area files = 7,014 bytes**. Both are comfortably inside the §6.7 soft target (~50 KB / ~15k tokens per part), so repetition costs nothing that matters. | **Repeat at file level, not at record level.** Every area part file carries the complete block as `meta.anwendungsbereiche_je_stufe`, keyed `SCH1..SCH4`, each entry tagged `bindung: "stufe"` — so a skill loading only `prim/su/geografisch.json` under the plan §5 B1 contract still sees all 40 items without reading a second file. This matches the existing "each part is self-contained with a full `meta` block" design from the 2026-07-28 shard-split row. **Explicitly rejected: copying the year's items onto each competence record.** That would make e.g. `AT.LP23.PRIM.SU.GEOGRAFISCH.SCH1.01` appear to own "Ich und Du und Wir", "Jahr und Zeit" and eight others, when the regulation attaches them to the whole school year across all six Kompetenzbereiche — a per-competence attribution the source does not make, and the precise failure mode the verbatim/provenance discipline exists to prevent. Same treatment for `bindung: "bereich"` (SEK1.D): the block is repeated in the one area file it belongs to, keyed by `(bereich, stufe)`, and not pushed down onto records. |
| 2026-07-30 | NOR40271471, SEK1.E, child 655 and 9 further stem paragraphs, found during the six-shard readiness audit | The competence stem paragraph is boilerplate (`Die Schülerinnen und Schüler können`) whose only role is to anchor the following `liste`; dropping it from the per-item `text` loses nothing | **False for SEK1.E.** The stem there carries a load-bearing performance condition that qualifies every competence in the block: child 655 reads verbatim `Die Schülerinnen und Schüler können, wenn sehr langsam, klar und deutlich in Standardsprache gesprochen wird,`. `_classify` correctly emits a `KOMPETENZSATZ` token for it, but `_kompetenzbereiche` — the handler for all five combined-heading subjects — has no branch for that token, so it is discarded with no issue logged. **Measured: 10 qualified stems in SEK1.E; 0 of 37 SEK1.E competences retain the qualifier in `text` or `text_roh`; 0 occurrences in the other five subjects.** These qualifiers are the CEFR performance conditions FINDINGS V-41 asks about, in prose form | **Decision: capture the stem sentence generally, not as a SEK1.E patch.** The stem is part of the competence as published for *every* subject and is currently stored nowhere at all — SEK1.E is simply the only subject where it varies and therefore the only one where the loss is visible. `Kompetenz` gains a `stammsatz` field holding the stem paragraph verbatim as published (the plain `Die Schülerinnen und Schüler können` for five subjects, the qualified form for SEK1.E), so a faithful quotation is `stammsatz` + the item `text`. Per-item `text` stays byte-unchanged, so the shipped SEK1.M item text does not move. The `kompetenzbezug` render block and the E4 access layer must quote `stammsatz` + `text`, never `text` alone. Rejected: prepending the qualifier into `text` (produces a leading-comma fragment and silently changes the meaning of an existing field), and a SEK1.E-only field (leaves the same latent hole for any future amendment that qualifies a stem in another subject). |
| 2026-07-30 | NOR40271471, the `Leistungsniveaus` passage in the general Mittelschule part, read while assigning differentiation axes for all six shards | Plan §4.7 assigns the `standard_standardplus` axis to **Mathematik** and `gers` (CEFR) to **the language subjects** (Deutsch, Lebende Fremdsprache) | The source says otherwise, verbatim: `In den differenzierten Pflichtgegenständen Deutsch, Mathematik und Lebende Fremdsprache erfolgt ab der 6. Schulstufe eine Unterscheidung nach den zwei Leistungsniveaus „Standard“ und „Standard AHS“.` The Leistungsniveaus axis covers **all three** differentiated Sek I subjects, and applies **from the 6. Schulstufe** (= K2–K4, not K1). CEFR is an *additional* axis for SEK1.E only (its own text carries A1/A2/B1 plus the stem qualifiers above), not a replacement for it. Separately measured: `Standard AHS` occurs 7× and **never** as a per-item marker — all occurrences are general prose about the differentiation procedure (**FINDINGS V-42 closed, negative**) | Source wins. `DIFFERENZIERUNGS_ACHSEN` is populated for all six: `SEK1.M`/`SEK1.D` → `standard_standardplus`; `SEK1.E` → `standard_standardplus` **plus** a `gers` sub-axis; `PRIM.D`/`PRIM.M`/`PRIM.SU` → `lehrplan_generisch` (no such axis exists in the Volksschule source). Every Sek I axis entry records `gilt_ab_stufe: "K2"`. Because V-42 is closed negative, `finde_differenzierung` must present `niveaus` as informative labels only and must not offer a per-item Standard-AHS filter; the only real per-item enrichment data is `allenfalls`, which is SEK1.M-only. The E7 "above" tier is therefore skill-authored elaboration on binding competence text, not a dataset query — plan §6.3's wording must be corrected accordingly. |
| 2026-07-30 | `schema/id_schema.py` `AREA_CODES["SEK1.D"]` vs. real parser output, six-shard readiness audit | The frozen area-code table is keyed on the area name the parser produces, so every shipped ID uses a frozen code (backlog E3-02: the scheme is frozen exactly once) | The `SEK1.D` table is keyed on the full heading `Integrativer Kompetenzbereich Sprachbewusstsein und Sprachreflexion`, but `bereich_re` strips the generic `Kompetenzbereich` scaffolding and extracts `Sprachbewusstsein und Sprachreflexion`. The frozen code `SPRACHREFLEXION` is therefore unreachable; the fallback slugifier mints `SPRACHBEWUSS`, and **12** items (measured against the live document, 2026-07-30) would ship under an unfrozen code. Only a soft `bereich_ohne_slug` issue is logged | **Decision D2 (taken 2026-07-30): re-key, keep the code.** `AREA_CODES["SEK1.D"]` is re-keyed to `"Sprachbewusstsein und Sprachreflexion"`; the code stays `SPRACHREFLEXION`, so no ID shape changes and nothing already shipped moves. Rationale: the extracted short name is also what ships as the teacher-visible `bereich_name`, and the convention is already "heading minus the generic scaffolding" for SEK1.M (`Zahlen und Maße`) and, where the scaffolding is a *suffix*, the full heading for PRIM.SU (`Sozialwissenschaftlicher Kompetenzbereich`). Rejected: widening `bereich_re` to keep the prefix — that would push boilerplate into a teacher-visible display name. `notes/id-schema.md` §3's table row must be corrected in the same change, since its "Area name (verbatim from RIS)" cell still shows the old key. **Follow-on guard:** every shipped `SubjectSpec` sets `bereich_slugs=id_schema.bereich_codes(band, fach)` by import, never a hand-copied dict, and a test asserts `SUBJECT_SPECS[k].bereich_slugs == AREA_CODES[k]` for all six so the two tables cannot drift. |
| 2026-07-30 | `parse_lehrplan.py:1235,1381` vs. `schema/id_schema.py::ANWENDUNGSITEM_AREA_FREI_ID_RE`, six-shard readiness audit | Application items always belong to a competence area, so an area segment is always available for the ID | For `bindung: stufe` (PRIM.D, PRIM.SU) the regulation attaches items to a whole school year and to no area at all. The parser mints the literal segment `ALLGEMEIN` instead — measured `AT.LP23.PRIM.SU.AB.ALLGEMEIN.SCH1.01` for the item *"Ich und Du und Wir"*, on all 37 PRIM.D and all 40 PRIM.SU items — asserting an area the source does not make. The area-free 7-segment form was already added to `id_schema.py` for exactly this case and is exhaustively tested, but nothing calls it. Nothing catches the invented code either: `ANWENDUNGSITEM_ID_RE` accepts `ALLGEMEIN`, and no validator rule requires an area code to exist in `AREA_CODES` | **Decision D3 (taken 2026-07-30): emit the area-free form; delete the `ALLGEMEIN` fallback.** `_make_id` gains an optional area segment; when the shard's binding is `stufe`, items get `AT.LP23.<Band>.<Fach>.AB.<Stufe>.<lfd>`. The `lfd` counter already scopes by `(stufe, bereich_name="")` and stays correct. The second, currently-unreachable `ALLGEMEIN` site on the `kompetenz`-binding path (line 1235) becomes a `ParseError` instead of a placeholder: for that binding an item with no area is a genuine structural anomaly, not an expected shape. A new **soft** validator rule reports any area code absent from the shard's `AREA_CODES` table, so a re-invented placeholder can never ship silently again. |
| 2026-07-30 | `schema/kompetenzen.schema.json` `$defs/anwendungsbereiche_je_stufe_eintrag`, reconciling two audits that proposed different shapes | The 2026-07-29 design row settled where `bindung: stufe` and `bindung: bereich` blocks live (repeat at file level, never pushed onto records), so the schema only needed the `stufe` shape it already has | The schema has **no** home for `bindung: bereich` (SEK1.D) at all: `$defs/anwendungsbereiche_je_stufe_eintrag.bindung` is a hardcoded `const: "stufe"`. Two audits proposed incompatible remedies — (a) relax that const to an enum and reuse the one key, since the area is implied by the file the block lives in; (b) add a parallel `meta.anwendungsbereiche_je_bereich` with a two-level `{bereich: {stufe: …}}` key. Also found: `$defs/kompetenzbereich.nummer` is `required` and typed `integer`, but is legitimately `null` for every area in five of six shards (measured: `None is not of type 'integer'`), so the schema currently rejects those five outright | **Decision: one key, self-describing entries, renamed.** `meta.anwendungsbereiche_je_stufe` becomes **`meta.anwendungsbereiche_bloecke`**, keyed by `stufe`, each entry `{bindung: "stufe"|"bereich", items: [...]}` and, when `bindung == "bereich"`, additionally `bereich_name` + `bereich_slug`. Rationale: the two block shapes are structurally identical, so two parallel `$defs` would duplicate a shape without adding information; but relying on "the area is implied by the file" (option a as proposed) would make a recombined document ambiguous, so the entry states its own area explicitly. The rename costs nothing shipped — SEK1.M is `bindung: kompetenz` and carries no such key — and avoids shipping a field whose name lies for SEK1.D. Separately, `$defs/kompetenzbereich.nummer` is widened to `["integer", "null"]` (it stays `required`, so the field must be present and explicitly null, matching the already-nullable `kompetenz.bereich_nummer`). `beispiel_kompetenzen.json` gains one example per novel shape rather than continuing to carry a PRIM.SU-shaped block inside a document whose own `meta.band` says `SEK1`. |
| 2026-07-30 | `validate_dataset.py::iter_records` vs. the 2026-07-29 file-level repetition decision, six-shard readiness audit | The validator's two hard rules (missing `id`/`stufe`/`text`; ID collision within a shard) apply to every shipped record | `iter_records` walks only `kompetenzbereiche[].kompetenzen[].anwendungsbereiche[]`, `zusatzkompetenzen[]` and `digitale_technologien_vorschlaege[]` — never `meta`. So for PRIM.D and PRIM.SU, whose application items live *only* in the meta block, neither hard rule applies at all: a malformed item there validates clean. Extending the walk naively is equally wrong — the 2026-07-29 decision deliberately repeats the identical block across every area file of a subject, so the ID-collision rule would fire on shipped-by-design data | **Decision: walk the meta blocks, and make the repetition rule explicit rather than accidental.** `iter_records` is extended to `meta.anwendungsbereiche_bloecke[*].items[]` for the missing-field hard rule and the soft reference/abbildung rules. For uniqueness, meta-block IDs go into a separate pool: a repeat across part files is **expected** and passes **only if the records are byte-identical**; a repeated ID whose content differs is a genuine corruption and stays a **hard** failure. The exemption is documented in the module docstring so it is not rediscovered later as a bug. |
| 2026-07-30 | `plugin/data/kompetenzen/sek1/m/*.json` and `index.json`, six-shard readiness audit | The shipped shard is protected by the existing suite, and the split-by-area layout satisfies plan §5's B1 contract (a skill loads the shard file directly into context, ~15k tokens per §6.7) | Neither holds. (a) **No test compares the shipped bytes to anything frozen** — `test_build_dataset.py` builds parts in memory and compares them only against themselves, and `test_validate_dataset.py` only schema-validates the shipped files, so any parser or build change could alter shipped Sek I maths data with the whole suite green. (b) `index.json` carries per-part counts and sizes but nothing mapping a search term to a part, so plan §5's own example `finde_kompetenz(fach=M, stufe=K2, stichworte=["Bruch"])` forces loading every part: **measured 177,576 bytes ≈ 44–45k tokens** for `sek1/m/`, against a ~15k target — and SEK1.M has the fewest area files of the six | Two decisions. **(1) Freeze the shipped bytes.** A checked-in `plugin/data/kompetenzen/<band>/<fach>/.sha256` manifest plus a test asserting every shipped file matches. Deliberate data improvements remain allowed — the guard exists to make them *visible and reviewed*, not to forbid them — so a change that legitimately alters output updates the manifest in the same commit, and a change that does not must leave it untouched. **(2) Add routing to `index.json` before the five remaining shards are built**, not after: a compact `stichwort_index` mapping normalised terms to part filenames, built at build time from competence and application text. Deferring this would mean regenerating and re-freezing SEK1.M's `index.json` a second time under an incompatible shape. |
| 2026-07-30 | `build_dataset.py` `meta` construction vs. what a consumer can observe, six-shard readiness audit | A skill loading a part file can tell why a competence has no application items | It cannot. `meta` carries `anwendungsbereiche_status` and `lehrstoff_quelle` but not the `anwendungsbereiche_bindung` axis, which exists only in the Python `SubjectSpec`. For SEK1.E (`prosa`) and PRIM.M (`keine`) there is no item anywhere to carry a per-item `bindung`, so a consumer cannot distinguish "the regulation offers only prose here" from "the build dropped the items". Related: nothing in the data says whether a subject has Bildungsstandards at all, so `finde_bildungsstandard_bezug`'s defined-empty result for Sachunterricht could only be a hardcoded subject exception in E4 | Both facts move into the data. `meta` gains **`anwendungsbereiche_bindung`** (required; enum `kompetenz\|bereich\|stufe\|prosa\|keine`, mirroring the `SubjectSpec` verbatim) and **`bildungsstandard_bezug`** (`verordnet` \| `keine_verordnung`), so downstream code branches on data rather than on a subject code. Consequence for E4: `finde_anwendungsbereiche`/`finde_lehrstoff` dispatch on `meta.anwendungsbereiche_bindung` and their signatures must accept `(fach, stufe, bereich)` as an alternate key, because `kompetenz_id` is `None` by design for three of the six shards; `nur_verbindlich=True` returns the same set as `False` wherever `allenfalls` never occurs, and must never be read as "0 non-binding items found". |
| 2026-07-29 | `tests/fixtures/sek1_deutsch.xml` (task P4, built to freeze the SEK1.D fixture required to regression-test the two-terminator row above), verified by two isolated-module experiments (the real `LehrplanParser` vs. a scratch copy of `parse_lehrplan.py` with the legend-table/`LEHRPLANZUSATZ` terminator branches deleted) run against the committed fixture and the live document alike | The 2026-07-28 row's “Hard-fail risk, not cosmetic” framing implies removing the terminator alone reproduces the `ParseError` ID collision the fixture is meant to guard against | It does not, for the `SubjectSpec` shape already established in `test_parse_lehrplan.py::_bindung_spec` (`kompetenz_sektion_re` matching the combined `Kompetenzbeschreibungen und Anwendungsbereiche, Lehrstoff (1. bis 4. Klasse):` heading). That heading text recurs verbatim inside the `LEHRPLANZUSATZ` appendix (child 543 in the live document); `Token.SEKTION_KOMPETENZ`'s handler resets `self.stufe = None` unconditionally wherever it fires, *including from inside `FACH_ANHANG`* (checked in `_step` before the per-state dispatch). With **only** the terminator removed, that reset (pre-existing, unrelated to P1/P2) fires at that second heading and blocks every subsequent `listelem` in the appendix from emitting an ID at all (`liste_ohne_kontext`, stufe is `None`) -- measured result: no crash, but `bereiche` silently grows from 4 to 6/7 (spurious `Hören`/`Sprechen`/`Linguistische Kompetenzen` areas minted from the appendix, since `Lesen`/`Schreiben` are reused by name and do not add new entries). The literal `ParseError` (`AT.LP23.SEK1.D.LESEN.K4.01` colliding, child 168 vs. 121) only reproduces if the stufe-reset is *also* removed -- a second, independent regression stacked on top of the terminator. | Not a correction to the terminator's necessity (§11.2 stands; the live document's counts are unaffected either way) -- a correction to the *failure mode* it guards against. `sek1_deutsch.xml` still carries the appendix span through its own `Kompetenzbereich Lesen`/`Schreiben` headings (children 515-556) precisely because it is the one fixture that can prove either regression, whichever form a future refactor takes: `TestNewSubjectFixtures::test_sek1_deutsch` asserts `bereiche == 4` (catches the silent-corruption path) inside a test that would also simply fail loudly on an uncaught `ParseError` (catches the collision path), without needing the monkeypatch scaffolding committed. See `notes/ris-xml-structure.md` §12 for the fixture's exact span. |
| 2026-07-31 | `parse_lehrplan.py::link_wiederholungen` (task E12-04, FINDINGS V-59), measured against both live documents (`resources/mittelschule/NOR40271471.xml`, `resources/volksschule/NOR40271469.xml`) via throwaway ad-hoc `SubjectSpec`s for the five unshipped subjects, area slugs wired from `id_schema.AREA_CODES` by import | `vorlaeufer`/`folge` and the `Wiederholen und Festigen` backlink lookup bucket competences on `(stufe, bereich_nummer)`; since only SEK1.M numbers its areas, `bereich_nummer` is `None` for every area in the other five shards, so all areas of a school year collapse into one bucket | Measured cross-area `vorlaeufer`/`folge` links before the fix: SEK1.M 0 (unaffected, numbered), SEK1.D 424, SEK1.E 382, PRIM.D 432, PRIM.M 446, PRIM.SU 720 (out of totals 638/522/600/600/864 respectively; max fan-out 19–24). SEK1.M was already correct because its area numbers and slugs agree 1:1 | **Bucket on the area slug instead.** `Kompetenz` and `Anwendungsitem` each gain a `bereich_slug: str` field, populated at every construction site from the same `self.bereich.slug` / `bereich_obj.slug or "ALLGEMEIN"` expression already used for `bereich_name` right next to it — area-less records get the same `"ALLGEMEIN"` sentinel and so still bucket together, matching prior `None` behaviour for them. `nach_stufe_bereich`, the `Wiederholen und Festigen` lookup and both `k.vorlaeufer`/`k.folge` now key on `(stufe, bereich_slug)`. Measured after the fix: **0 cross-area links for all six shards.** SEK1.M's shipped bytes are unchanged (`test_shipped_bytes.py` stays green; slug-based bucketing was already proven equivalent for SEK1.M — 0 differences across its 42 competences, `GZINTEGRATIV` stays alone in its bucket either way). `join_anwendungen` (the separate SEK1.M-only text-repetition join, keyed on `bereich_nummer` at different call sites) is out of scope — it returns early for every `anwendungsbereiche_bindung != "kompetenz"` and was never the bug. |
| 2026-07-31 | `parse_lehrplan.py::_make_id` while implementing decision D3 (task E12-05), measured against both live documents via throwaway ad-hoc `SubjectSpec`s for the five unshipped subjects | `_make_id` can simply delegate to the frozen constructors `id_schema.format_id`/`format_item_id`, which already build both the area-bearing and the area-free form and self-check by round-tripping through `parse_id` | Full delegation breaks the test suite: 22 errors, `IdSchemaError: unknown fach 'X'`. The containment-bindung fixtures in `tests/test_parse_lehrplan.py::_bindung_spec` deliberately use `band="TEST"`/`fach_code="X"`, which are **not** in `id_schema`'s frozen `BAND_FAECHER` table — that exclusion is intentional, so fixture IDs can never collide with or pollute the real shard ID space | **Hybrid, guarded by `id_schema.ist_gueltige_kombination(band, fach)`.** For the frozen six shards `_make_id` delegates and inherits the round-trip self-check; for anything outside that set it hand-builds the identical grammar, mirrored string-for-string. Alternatives rejected: (a) adding `TEST`/`X` to the frozen `id_schema` tables — pollutes the shipped ID space with test-only values to serve a test convenience; (b) re-keying every fixture spec onto real band/fach pairs — loses exactly the isolation the `TEST` band was introduced for. Consequence: the delegating path must stay covered by tests using **real** band/fach (`TestE1205AreaFreeItemIds` uses `PRIM.D`/`SEK1.D`), otherwise the production path would be exercised by no test at all. Note the two `bereich_slug` values are now deliberately distinct and must not be unified: the **record field** keeps the `"ALLGEMEIN"` sentinel (E12-04 progression bucketing), while the **ID segment** (`id_bereich_slug`) is `None` when no area is known. |
| 2026-07-31 | `parse_lehrplan.py::_kompetenzbereiche`/`_kompetenz_gz_integrativ`/`_anwendungsbereiche` (task E12-06, FINDINGS V-58/V-68), measured against both live documents via the same ad-hoc `SubjectSpec` harness as the two rows above, plus the shipped `sek1_mathematik.xml` fixture | The 2026-07-30 stem-decision row (above) established that `stammsatz` must be captured for every subject; V-68 separately noted `_kompetenzbereiche`/`_anwendungsbereiche` have no fallback branch, so any token neither handles vanishes with no `ParseIssue` | Measured after capture: SEK1.E goes from 0/37 to **all 10** `KOMPETENZSATZ` occurrences (8 distinct sentences — two recur across separate class-year blocks) present verbatim in `stammsatz`, including the full trailing-comma form quoted in the 2026-07-30 row. Empty-`stammsatz` count is **0** for SEK1.M/SEK1.E/PRIM.M/PRIM.D/PRIM.SU. SEK1.D measured **4 of 41** competences (`AT.LP23.SEK1.D.LESEN.K2.01-04`) with `stammsatz` empty and a newly-logged `kompetenz_ohne_stammsatz` issue — a genuine source anomaly, not a bug in this fix (see next row). SEK1.M shipped bytes are unaffected: 0 diffs across the 42 competence + 237 item `text`/`text_roh` values against the pre-change parser, and `test_shipped_bytes.py` stays green, because `build_dataset.py`'s key whitelist does not emit `stammsatz` yet (deliberately deferred to E12-11/E12-16). Issue-count deltas: SEK1.M 4→4, SEK1.D 0→1 (the new issue above), SEK1.E 4→4, PRIM.M/PRIM.D/PRIM.SU 0→0 — the V-68 fallback added no noise on any of the six shards as they stand today | `Kompetenz` gains `stammsatz: str`, the stem paragraph verbatim (`ev.extracted.text`, never the `strip_stem`-ed `rest`), captured by new `STAMMSATZ`/`KOMPETENZSATZ` branches in `_kompetenzbereiche` and `_kompetenz_gz_integrativ` (the latter's own docstring used to call the stem "ignored, same as in KOMPETENZBEREICHE" — now captured there too, so SEK1.M's 2 promoted GZ competences carry one like the other 40). Tracked as `self._stammsatz`, reset to `None` at every `STUFE`/`BEREICH` boundary and on section entry/exit (the `SEKTION_*`/`FACH_UEBERSCHRIFT`/`LEHRPLANZUSATZ`/legend-table-terminator transitions, plus `_verlasse_sektion`) so a stem can never leak across a boundary; it stays open across the list(s) it introduces, matching SEK1.E's one-stem-governs-a-whole-block shape. A competence emitted with no stem open gets `stammsatz=""` **and** a logged `kompetenz_ohne_stammsatz` issue, never a silent empty string. Separately (V-68): `_kompetenzbereiche` and `_anwendungsbereiche` each gained an explicit, commented ignore-set (`Token.TEXT`, `Token.IGNORIEREN` only — ordinary prose and non-interpreted element types) plus a fallback branch logging `unbehandeltes_token_kompetenzbereiche`/`unbehandeltes_token_anwendungsbereiche` for anything else. `_anwendungsbereiche`'s pre-existing `STAMMSATZ`/`KOMPETENZSATZ` branches are untouched — different semantics there (SEK1.M's text-repetition join). `text`/`text_roh` stay byte-identical everywhere (0 diffs measured) — the stem is never prepended into them, per the 2026-07-30 row's rejected-alternative. |
| 2026-07-31 | NOR40271471, SEK1.D `Kompetenzbereich Lesen` / 2. Klasse, children 464-465 — surfaced by the new `kompetenz_ohne_stammsatz` issue while measuring the E12-06 row above against the live document | Every (subject, stufe, bereich) list is preceded by its own stem `absatz` (`Die Schülerinnen und Schüler können[, ...]`), the shape every other combination measured across all six shards uses | For this one area/stufe the source has **no separate stem `absatz` at all**: child 464 (`Kompetenzbereich Lesen` heading) is followed directly by child 465, a `<liste>` whose *first* `<listelem>` is itself the stem. **The source text is complete and well-formed** — verbatim `<listelem ct="text"><symbol stellen="3">Die</symbol>Schülerinnen und Schüler können</listelem>` — but `element_text` drops all `<symbol>` content unconditionally as "the list bullet glyph, presentation not text" (`parse_lehrplan.py` line ~120/149), which is right for the `stellen="1"` bullet `–` and **wrong here**, where `stellen="3"` carries the real word `Die`. The parser therefore sees `Schülerinnen und Schüler können`, which `STEM_RE` does not match, so the stem is not recognised as one. Confirmed by direct XML inspection of the raw element (orchestrator spot-check, 2026-07-31), correcting the subagent's original reading of this row as a source defect. Consequence, pre-existing and unrelated to E12-06: `_emit_kompetenzen` correctly has no stem open (hence the new issue) but still emits that fused fragment as if it were a real competence, `AT.LP23.SEK1.D.LESEN.K2.01` with `text == "Schülerinnen und Schüler können"` — a competence ID whose text is a meaningless fragment. **Two** occurrences of the symbol-carried-`Die` shape in NOR40271471, both under a `Kompetenzbereich Lesen` heading: child 465 (inside the parsed SEK1.D content) and child 552 (inside the `LEHRPLANZUSATZ` appendix, which the parser deliberately terminates before, so it is not parsed). All other (stufe, bereich) combinations across all six shards have a proper separate stem `absatz`. Separately, `<symbol>` carries non-bullet enumeration labels (`1)`, `2.1)`, `1.`, `Siehe`) at children 205-390 and 2132, all outside the six shards' competence sections and so harmless today — but the same unconditional drop applies to them | **Not fixed here — logged, out of scope for E12-06.** This is a **text-extraction defect of the same class as V-58** (load-bearing source text silently deleted), not the missing-element source anomaly the subagent first recorded, and not an unhandled-token problem. The fix belongs in `element_text`: drop `<symbol>` only when it is a bullet glyph, keep it when it carries real words. **Do not** "fix" it by special-casing a bare-stem first `listelem` — that would paper over a general extraction bug with a shape-specific patch and leave the enumeration labels still being deleted. **Predicted consequence, to be measured when it lands:** with `<symbol>` handled, child 465's first `listelem` reads `Die Schülerinnen und Schüler können`, matches `STEM_RE`, classifies as `STAMMSATZ` rather than a competence — so **SEK1.D drops from 41 to 40 competences**, `LESEN.K2.02-04` renumber to `.01-.03`, and all 4 gain a proper `stammsatz`. **The frozen `ERWARTET` count of 41 for SEK1.D therefore encodes this bug and must be re-measured, not trusted**, before E12-08 wires the expected-count tables and before E12-16 builds the shard. SEK1.D is not registered or shipped yet (E12-08), so nothing ships wrong today. |
| 2026-07-31 | Full <symbol> inventory in NOR40271471 and NOR40271469 while implementing E12-06a / V-69 | The V-69 diagnosis treated stellen as the bullet/content discriminator: stellen="1" means the presentation dash and other values carry real content | Measured across both full documents: 2,977 instances are <symbol stellen="1">–</symbol>, but one additional presentation bullet is <symbol stellen="2">– </symbol>. Non-bullet content includes Die, Siehe, and enumeration labels (1), 2.1), 1. and variants) under several stellen values. Attribute value alone is therefore not a complete discriminator | **Use symbol content, not stellen, as the deletion rule.** element_text drops a symbol only when its trimmed textual content is exactly the standalone dash – or -; every word and enumeration label survives regardless of stellen. When a retained symbol word and an adjacent alphanumeric tail have no literal XML whitespace (Die</symbol>Schülerinnen), extraction restores that lexical boundary while preserving source order. Tests cover the live stellen="2">– </symbol> outlier, a word deliberately carrying stellen="1", and a retained enumeration label. This supersedes only the discriminator sentence in the preceding V-69 row; its raw-source diagnosis remains valid. |
| 2026-07-31 | parse_lehrplan.py::_classify / _emit_kompetenzen, E12-06a, measured first against the committed SEK1.D fixture and then both full live documents | Once element_text retains Die, child 465's first listelem will automatically classify as STAMMSATZ and the predicted 41 → 40 count follows without another parser change | The sequential state machine classifies only top-level abschnitt children; at child 465 it sees the enclosing liste and emits one Token.LISTE. _emit_kompetenzen then iterated descendant listelems directly, so the restored text was still emitted as a competence and the count stayed 41. The handover prediction omitted this control-flow layer | **Classify every competence-list item semantically, independent of list position.** _emit_kompetenzen routes each listelem through the existing strip_stem/STEM_RE classification; a bare STAMMSATZ opens the verbatim stem for following entries in source order and is not emitted as a competence. This is additional to, not a substitute for, the general element_text fix and is not keyed to child 465 or index zero. Qualified/full KOMPETENZSATZ list items retain their prior competence semantics; no live instance required broadening that case. Measured after the fix: SEK1.D **4/40/16/54**, LESEN.K2.01-.03 are the three real competences and all carry Die Schülerinnen und Schüler können; all six subjects have 0 empty stems and 0 kompetenz_ohne_stammsatz issues. Full suite 417 → **419**. In-memory canonical serialization of SEK1.M parser output is byte-identical before/after (210,376 bytes, SHA-256 b387fe34a9fc5bd95a2b02001d2e13f8337a9fddc74a1c10dc9f29294d7461fc); the shipped-byte guard remains green. |
| 2026-08-02 | All five non-SEK1.M section headings, committed fixtures and both full live documents (E12-08) | Plan §4.4/§4.5 leave `anwendungsbereiche_status` and `lehrstoff_quelle` to be assigned per subject; the plan's own enum for status is `optional_sektion \| item_flags` | Both axes are determined by measurement, not choice. **`lehrstoff_quelle`:** the section heading discriminates. Four subjects read `Kompetenzbeschreibungen und Anwendungsbereiche, Lehrstoff (…)` and carry their Lehrstoff in the Anwendungsbereiche items; **PRIM.M alone** reads `Kompetenzbeschreibungen, Lehrstoff (…)` with no Anwendungsbereiche anywhere, so its Lehrstoff cannot come from them. **`anwendungsbereiche_status`:** `item_flags` is defined by the schema as "every item carries a real `verbindlich` flag", and the `allenfalls` marker producing that flag is SEK1.M-only (already measured; `allenfalls_pruefen=False` for all five). | **DECIDED + DONE.** SEK1.D/E, PRIM.D, PRIM.SU → `optional_sektion` + `aus_anwendungsbereichen`. PRIM.M → `keine` (V-24) + `eigen_ausgewiesen`. Rejected `item_flags` for the four: it would advertise a binding split that is always 0/N, and downstream `nur_verbindlich=True` would look like a filter that found nothing rather than one that does not apply. **Consequence, logged as FINDINGS V-45 and not resolved here:** nothing extracts PRIM.M's separately-stated Lehrstoff, so that axis currently points at content the dataset does not carry. Decide before PRIM.M ships. |
| 2026-08-02 | `parse_lehrplan.result_to_dict` vs. `ParseResult.bloecke` (E12-08) | The backlog asks result_to_dict to "emit bloecke"; the obvious reading is `dataclasses.asdict` over each block, mirroring the other three lists | `Anwendungsblock.items` is a `list[Anwendungsitem]`, and every one of those items already appears in full under the sibling `anwendungsitems` key. A plain `asdict` would therefore duplicate the entire application payload inside the parser's serialisation — 237 items for SEK1.M, 54 for SEK1.D — for no added information | **DECIDED + DONE.** Blocks are emitted under `anwendungsbereiche_bloecke` with all scalar fields intact and `items` reduced to a list of item **ids** (`_block_to_dict`). Membership and ordering — the whole reason blocks matter for the `bindung: bereich`/`stufe` shards, where the block rather than the competence is what items attach to — are preserved, and the bodies stay single-sourced. Verified additive: rebuilding SEK1.M after the change is byte-identical to the shipped shard, so the shipped-byte manifest is untouched. Note the guard that did **not** catch this: `test_shipped_bytes.py` only hashes files on disk and never rebuilds, so a build-output change is invisible to it — a rebuild-and-diff is required whenever `result_to_dict` changes. |
| 2026-08-02 | `build_dataset.py` routing vs. `ParseResult.bereiche`, all six shards from the committed fixtures and both live documents (E12-09, V-59) | The build's number-keyed routing is one bug at one site: `build_parts` raising `KeyError: None` for unnumbered subjects | It is one rule applied at **four** sites, and only one of them announced itself. (a) `build_parts` keyed `kompetenzen_je_bereich` on `b.nummer` — the visible `KeyError: None`. (b) `_kompetenz_zu_dict` derived `ist_zusatz` from `bereich_nummer is None`, which for the five unnumbered shards is true of *every* competence: all 205 would have been written as zusatzkompetenzen carrying a null `bereich_nummer` and omitting the `anwendungsbereiche` key. (c) `combine_parts` sorted on `(b["nummer"] is None, b["nummer"])`; once every area is unnumbered the tuples tie on the first element and Python compares `None` to `None`, raising `TypeError`. (d) `zaehle` counted `zusatzkompetenzen` on the same null test, so the build report would have disagreed with the files actually written. Sites (b)–(d) are silent or wrong-but-plausible, not crashes; none was reachable while SEK1.M was the only registered spec | **DECIDED + DONE.** One identity rule, applied at all four: a competence belongs to an area iff its `bereich_slug` is in `{b.slug for b in result.bereiche}`, factored into `_offizielle_slugs()` and shared by `build_parts` and `zaehle` so the report cannot drift from the output. `ist_zusatz` is now decided by the caller and passed into `_kompetenz_zu_dict`, never re-derived. The sort key gained a total third element (`slug`), ordering numbered areas first and unnumbered ones by slug. `zusatzkompetenzen` now means *belongs to no official Kompetenzbereich*: **2** for SEK1.M (GZINTEGRATIV, V-57), **0** for the other five. Measured: all six build, 42/40/37/40/40/48 competences with zero lost or duplicated IDs, areas 4/4/4/4/4/6; SEK1.M rebuilds byte-identical. Only `GZINTEGRATIV` is a warning-free route to zusatzkompetenzen — the `ALLGEMEIN` sentinel deliberately still warns, since a competence attributable to no area is a structural surprise that should stay visible (measured: no competence carries it in any shard, so this adds no noise). New `tests/test_build_six_shards.py` covers all six from the **committed fixtures**, so build breadth no longer depends on the gitignored `resources/`. |
| 2026-08-02 | `meta.anwendungsbereiche_bloecke` key shape vs. the measured SEK1.D block set (E12-10) | The schema's own description: the container is "Keyed by stufe (SCH1..SCH4 for PRIM; K1..K4 for SEK1.D)" | That key is unique for `bindung: stufe` (PRIM.D, PRIM.SU: exactly one block per school year, measured 4 blocks each) but **collides four ways for `bindung: bereich`**. SEK1.D has **16** blocks — four per class year, one per area (`Zuhören und Sprechen`, `Lesen`, `Schreiben`, `Sprachbewusstsein und Sprachreflexion`), measured 2026-08-02. Within one area part file the year key is unique, because that file carries only its own area's four blocks; the recombined document, which holds all four areas, would silently keep one area per year and drop three — 40 of SEK1.D's 54 items. | **DECIDED + DONE.** For `bindung: bereich` the key is `<BEREICH_SLUG>.<STUFE>` (`LESEN.K1`); `bindung: stufe` keeps the plain school year. Part files and the recombined document therefore share one shape, and `combine_parts` merges with a single dict update that both de-duplicates the stufe case (the same block repeated in every area file) and unions the bereich case (disjoint areas). The area is independently stated inside each entry via the schema-required `bereich_slug`, so the composite key is redundant-but-unique, not load-bearing. Conflicting content under one key raises `BuildError` rather than picking a winner. The schema's `additionalProperties` carries no `propertyNames` constraint, so this validates unchanged; only the description was corrected, in the same commit. Measured after: recombination yields every item exactly once — SEK1.M 237, SEK1.D 54, PRIM.D 37, PRIM.SU 40 — and all six shards validate against `kompetenzen.schema.json`. |
| 2026-08-02 | `_kompetenz_zu_dict`'s `anwendungsbereiche` key across the binding axes (E12-10) | The key is emitted for every competence under an official area, "possibly empty", as the pre-E12-10 comment stated | Correct only for `bindung: kompetenz`, where the source really does attach items per competence and an empty array honestly means "none for this one". Under `bereich`/`stufe` the items attach to an area or a whole school year across all competences, so an empty per-competence array would assert that the regulation looked for items for *this* competence and found none — an attribution it never makes. It would have appeared on all 128 competence records of SEK1.D/PRIM.D/PRIM.SU | **DECIDED + DONE.** The key is emitted only when items really were joined, or when the shard's binding axis is `kompetenz`; `_kompetenz_zu_dict` takes that as an explicit `kompetenz_gebundene_items` argument rather than inferring it. Under the coarse axes no competence record carries `anwendungsbereiche` at all — the items are reachable exclusively through `meta.anwendungsbereiche_bloecke`. SEK1.M is unaffected and rebuilds byte-identical. `zusatz.json` deliberately carries no block container either: it holds no Kompetenzbereich, and the plan section 5 B1 contract is about a skill loading an *area* part. |
| 2026-08-02 | `DIFFERENZIERUNGS_ACHSEN` vs. NOR40271471 and `resources/bildungsstandards/NOR40255561.xml` (E12-11, V-61) | Plan §4.7 assigns `gers` to "the language subjects" and `standard_standardplus` to mathematics alone; five of six shards had no registered axis and fell through to the warned generic fallback | The source says otherwise, verbatim: *"In den differenzierten Pflichtgegenständen Deutsch, Mathematik und Lebende Fremdsprache erfolgt ab der 6. Schulstufe eine Unterscheidung nach den zwei Leistungsniveaus „Standard“ und „Standard AHS“."* All three Sek I subjects carry `standard_standardplus`, from the 6. Schulstufe (K2–K4, never K1). Bildungsstandard coverage measured against the BiSt regulation: **"Sachunterricht" occurs 0 times**; Deutsch (4), Mathematik (4), Lebende Fremdsprache (1) and Englisch (2) all occur. | **DECIDED + DONE.** All six axes registered explicitly: `standard_standardplus` + `gilt_ab_stufe: "K2"` for SEK1.M/D/E, `lehrplan_generisch` for the three primary shards. The primary shards are *registered* rather than left to the fallback, so the fallback warning keeps meaning "nobody has looked yet" instead of "this subject genuinely has no axis". `meta.bildungsstandard_bezug` is `verordnet` for five shards and `keine_verordnung` for PRIM.SU, so E4-05 reads a data fact instead of hardcoding the Sachunterricht exception. `meta.anwendungsbereiche_bindung` now mirrors the `SubjectSpec` verbatim. Measured after: zero fallback warnings across all six, all six schema-valid. |
| 2026-08-02 | The `gers` sub-axis for SEK1.E (E12-11) | CEFR levels could be attached per class year, since V-11 recorded that A1/A2/B1 are present in the Mittelschule document | **Zero** CEFR tokens occur in SEK1.E's competence texts or stems (measured across all 37 competences): the V-58 qualifiers are prose performance conditions (*"wenn sehr langsam, klar und deutlich in Standardsprache gesprochen wird"*), not CEFR labels. The levels live in two other places: a subject-level statement (*"orientiert sich an den … Kompetenzniveaus A1 und A2 sowie an ausgewählten Deskriptoren des Kompetenzniveaus B1"*) and a **per-class-year** prose paragraph (*"In allen vier Kompetenzbereichen wird das Zielniveau A2+ angestrebt"*, values A1/A2, A2, A2+, and "A2+ mit ausgewählten Deskriptoren aus B1"). Nothing extracts either. | **DECIDED + DONE.** The `gers` sub-axis carries only what the regulation states at subject level — `niveaus: ["A1","A2","B1"]` — plus an explicit `je_stufe_ausgewiesen: false`. The per-class-year mapping is **deliberately not synthesised**: it exists verbatim in the source but is not extracted, and inventing the year→level assignment would assert an attribution the dataset cannot support. Recorded against **V-41**, which this measurement partly answers: the paragraphs exist and are quotable, so V-41 is now an extraction task, not an open question about whether the data exists. Extraction stays E8. |
| 2026-08-02 | SEK1.M part sizes after `kompetenz.stammsatz` starts shipping (E12-11) | E12-16 predicted every part inside the §6.7 soft target ("all pass, 9–25 KB / 2.3–6.3k tokens measured") | Shipping the stem pushes `zahlen.json` from 49,905 to **50,609 bytes**, past the 50,000-byte soft target — it had only 95 bytes of headroom (recorded 2026-08-02, E12-08). Per-part deltas: daten +574, figuren +899, variablen +899, zahlen +704, zusatz +241, index +119. The stem is near-pure duplication in SEK1.M: **42 records, 1 distinct stem**, the same 35-character string every time. `build_dataset.py` correctly reports `REVIEW`, which is a sharding trigger and never a build failure (E3-07). | **Logged, not fixed here — and E12-16's prediction is corrected.** Hoisting `stammsatz` to `meta` would remove the duplication but breaks the contract `$defs/kompetenz.stammsatz` states and that E12-16 promotes to `required` *per record*: a faithful quotation is `stammsatz` + `text`, and a consumer holding one record must be able to produce it without reading `meta`. The field therefore stays per-record. Resolving the byte target belongs to the tasks that own sharding and the rebuild (**E12-12**, **E12-16**); a plausible route is finer splitting, since `zahlen.json` is a single Kompetenzbereich and §6.7's own prescribed remedy is to split finer. **Shipped bytes on disk are untouched** — E12-16 does the rebuild and re-freeze — so `test_shipped_bytes.py` stays green while the shipped shard and the build output legitimately diverge until then. |
| 2026-08-02 | `validate_dataset.py` record traversal and ID pool vs. the six built shard shapes (E12-14) | The validator's rules, written when SEK1.M was the only shard, generalise to the other five once those shards exist | Two gaps, both silent. (a) `iter_records()` never walked `meta.anwendungsbereiche_bloecke[*].items[]`, so under `bindung: bereich`/`stufe` — where **all** application items live in that container — a missing required `id`/`stufe`/`text` went entirely undetected; that is every one of SEK1.D's 54, PRIM.D's 37 and PRIM.SU's 40 items. (b) The flat ID-collision rule would have fired on legitimate data: under `bindung: stufe` the complete block is deliberately repeated verbatim in every area part file (decision of 2026-07-29), so the same item id correctly appears in four or six parts. | **DECIDED + DONE.** (a) Block items are now yielded by `iter_records()` and participate in every record-level check, tagged `is_stufe_block`. (b) The collision rule is repetition-aware rather than weakened: if *every* occurrence of an id is a `stufe`-block item, identical content across parts passes and **differing content is hard**; if any occurrence sits outside that pool the unmodified plain-collision rule applies. `bindung: bereich` (SEK1.D) deliberately gets **no** tolerance — its per-area blocks are disjoint, so a repeated id there is a real collision, and a dedicated test asserts it stays hard even with identical content. Five soft rules added (unknown area code, `kompetenz_id` set where the binding forbids it, area-free ID outside `bindung: stufe`, `verbindlich: false` outside SEK1.M, counts vs `parse_lehrplan.ERWARTET_BY_SPEC` imported rather than re-typed). Hard/soft split unchanged per plan §0.1: hard only for ID collision and missing required field. Every rule has both a passing and a failing fixture. **No new rule fires on real data** — the shipped SEK1.M shard still reports 0 hard / 0 soft / 0 info. Orchestrator spot-check: dropping one competence from a copy of the shipped shard makes the count rule report `kompetenzen: expected 42, measured 41`, so it is not vacuous. |
| 2026-08-02 | `test_strict_never_promotes_informational_findings`'s fixture placement (E12-14) | The fixture was written under `sek1/m` while its document declares `fach_code: "ZZ"` — an unrecognised subject code, which is the whole point of that test | The directory placement and the document contradicted each other. Harmless until E12-14 added the frozen-count and area-code rules, which key on the *directory's* band.fach: a deliberately minimal one-competence fixture then also tripped both, burying the unknown-enum assertion the test exists for. | **DECIDED + DONE.** The fixture moves to `sek1/zz`, making the placement agree with the document's own `fach.code` and keeping it outside the six frozen shards, where the count and area-code rules correctly do not apply. The test still asserts exactly what it always did — that an unrecognised enum value in a tolerant position is never promoted to a failure under `--strict` (E2-16). This is a fixture-consistency fix, not a relaxation: both new rules keep their own dedicated pass/fail tests. |
| 2026-08-02 | `build_index`'s `meta` copy after E12-10 shipped `meta.anwendungsbereiche_bloecke` (found while integrating E12-12) | `index.json` is the cheap discovery file: `meta` + per-part counts and sizes, read first to decide which part to load | E12-10 made `meta` per-part and put the full verbatim application-item container in it. `build_index` copies a part's `meta` wholesale, so **every** application item was duplicated into the discovery file — measured: 20,460 of PRIM.D's 23,382-byte index `meta`, 12,505 of PRIM.SU's, 7,035 of SEK1.D's. A reader paid for the items before choosing a part, which is precisely what the file exists to avoid. Introduced by E12-10 and not noticed there; the shipped SEK1.M shard is unaffected (it has no blocks). | **DECIDED + DONE.** `build_index` strips `anwendungsbereiche_bloecke` from its `meta` copy. Nothing is lost: every area part file carries the complete container, which is where a consumer reads it (plan §5 B1). Measured index sizes after: PRIM.D 37,175 → **14,571** (−61%), PRIM.SU 29,607 → **15,194**, SEK1.D 27,460 → **19,513**; SEK1.M unchanged at 28,027 (no blocks). Lesson for future `meta` additions: `build_index` copies `meta` by reference to a part's, so anything added there lands in the discovery file too — check the index size whenever `build_meta` grows. |
| 2026-08-02 | `stichwort_index` term selection and postings encoding (E12-12, V-67) | A term -> part index is straightforwardly compact | Two measured pressures. (a) Terms occurring in *every* part carry no routing information: SEK1.M has 23 such terms plus 31 more in 4 of 5 parts, all boilerplate; on the `stufe`-bound shards it is far worse — 169 of PRIM.D's 480 candidate terms and 94 of PRIM.SU's 440 sit in every area part purely because the application block is repeated verbatim per file. (b) The index dominates the file it lives in: 23,476 of SEK1.M's 28,027 index bytes. | **DECIDED + DONE.** Terms shorter than 4 characters, a curated German stopword list, and any term whose postings would span more than **2** part files are dropped outright (dropped, not truncated — a term in 3+ of 5 parts routes nowhere useful). A cap of 1 was measured (541 terms, 21,990 bytes) and **rejected**: it drops `bruch` itself, which legitimately spans `daten.json` + `zahlen.json`. Postings are a comma-joined string rather than a JSON array, measurably smaller under the existing `indent=2` dump. Rejected: encoding postings as indices into `teile` (smaller, but forces the consumer to cross-reference instead of reading a self-describing filename) and a noun-capitalisation heuristic (measured ~5% saving; German nominalised infinitives such as "Arbeiten"/"Auswählen" are legitimately capitalised mid-list in the source, so the heuristic is fragile for little gain). Result: 673/407/172/284/154/292 terms across the six shards, deterministic and fully sorted. `index.json` remains inside the §6.7 per-part soft target (28,027 bytes ≈ 7k tokens, target 50,000 / 15k), so this is not a breach — but it is ~8× the pre-E12-12 index and is read on every query; recorded for E12-16's size report alongside the `zahlen.json` row. A considered alternative, should that review want it: move `stichwort_index` to its own file so an area-name lookup need not pay for it. |
| 2026-08-02 | `build_dataset.py --verify` across the six registered specs (E12-13) | `--verify` guards the build against a count regression | It guarded exactly one shard. `soll = ERWARTET_SEK1_M if args.spec == "SEK1.M" else {}` — and an empty expected table produces no mismatches, so `--verify` printed **VERIFY OK** for five of the six without comparing anything. The same vacuous pattern sat in `build_report`. A second, independent `DEFAULT_SOURCES` table also listed only SEK1.M, so the other five could not be built without an explicit `--source`. | **DECIDED + DONE.** `ERWARTET_BUILD` holds all six tables in the build's own vocabulary, **measured 2026-08-02** against both live documents and reproduced exactly from the committed fixtures (they agree on every key for all six, so a fresh clone verifies the same numbers). An empty or missing table is now an explicit failure — "no expected build counts for X", exit 1 — because silence is the one thing a verifier must never do. `DEFAULT_SOURCES` is deleted in favour of delegating to `parse_lehrplan.default_source()`; two band→document tables would be two things to keep in step, and a test asserts the second one has not come back. Proven non-vacuous: injecting a one-record regression into each spec's table makes `--verify` exit 1 for **all six** individually, and a parametrised test pins that. |
| 2026-08-02 | Shipped SEK1.M vs. a current rebuild, audited before E12-16 (E12-13) | After E12-11 and E12-12 the shipped files necessarily differ from what the build now produces; the divergence needs to be known, not discovered during the re-ship | Audited key-by-key. The rebuild adds exactly three things and nothing else: `meta.anwendungsbereiche_bindung` + `meta.bildungsstandard_bezug` (E12-11) on every part, `stammsatz` on every competence record (E12-11), and `index.json`'s `stichwort_index` (E12-12). **Verbatim `text` is byte-identical across every record of every part.** | **Logged as the E12-16 pre-audit.** Shipped bytes on disk stay untouched and `test_shipped_bytes.py` stays green; E12-16 rebuilds, re-freezes the manifest and reviews the diff line-by-line, and this row is the expected-diff list it should check against. Anything in that diff beyond these three additions is a regression, not an improvement. |
| 2026-08-02 | `LehrplanParser._classify` check order (E12-15, V-55) | V-55 records that in the primary band a level marker and an area heading share an element type, so the order of checks is load-bearing — but nothing tested it | Confirmed **latent, not live**: run against every real `ueberschrift/@typ="erll"` element in all six committed fixtures, the `STUFE_RE`-first and `bereich_re`-first orderings agree everywhere, because today's area regexes happen not to match a `"1. Schulstufe:"` heading. So no data-driven test can catch a reordering — the hazard only materialises when a future `bereich_re` becomes more permissive, which is exactly how V-55 predicts it would break. **Orchestrator verification:** the real `_classify` was temporarily reordered (`bereich_re` moved above `STUFE_RE`) and the full suite re-run — **691 of 694 tests still passed**, and only the three dedicated V-55 tests failed. The parser file was restored and the restoration confirmed by `git diff`. | **DECIDED + DONE.** `tests/test_classify_order.py` guards the order at three levels: a control asserting the **real, unmodified** `_classify` returns `Token.STUFE` for a class-year heading even under a deliberately permissive trap `bereich_re` (this is the non-circular one — it fails if production is reordered); a comparison against a swapped reproduction; and an end-to-end test that monkeypatches `_classify` at class level (restoration asserted) and shows the swap silently drops a competence with a `liste_ohne_kontext` issue. A fourth test pins the "both orderings agree on all real headings today" measurement, which is what makes the trap-spec tests necessary rather than redundant. |
| 2026-08-03 | All six shipped shards after the E12-16 build; §6.7 of the plan | Every part file within the §6.7 soft target of 50,000 bytes / 15,000 approx. tokens | **37 of 38 shipped files pass; exactly one exceeds.** SEK1.M `zahlen.json` is **50,608 bytes / ~12,652 tokens** — 608 bytes (**1.2%**) over the byte target, comfortably inside the token target. The overage is the direct, predicted cost of E12-11 shipping `kompetenz.stammsatz` to every record: the part had 95 bytes of headroom at 49,905 before it. Every other part passes with margin; the next largest is PRIM.D `rechtschreiben.json` at 40,187 bytes. Full-directory totals (not a target, the parts are the unit): SEK1.M 213,265 · SEK1.D 97,739 · SEK1.E 51,700 · PRIM.D 153,103 · PRIM.M 49,919 · PRIM.SU 158,373 bytes; 724,099 bytes over 38 files. | **DECIDED + DONE — sharding review held, outcome "accept".** §6.7 defines exceeding the target as a *review trigger, never a build failure*, and `build_dataset.py` implements it as a soft finding by design (E3-07). The review was held and the overage is accepted as shipped. **Rejected: splitting `zahlen.json` finer**, which the E12-16 task text had proposed as the remedy. Splitting an area part breaks the one-part-per-`Kompetenzbereich` invariant that `index.json`'s `teile[].typ`, the validator's part rules, and the B1 access contract ("a skill loads *an area* part") all rest on — a structural change to the shipped layout and to E4's dispatch, in exchange for 608 bytes. The token budget, which is what actually governs context cost, is at 84% of target. Confirmed by the project owner 2026-08-03. `validate_dataset.py` reports this as its single soft finding; `test_real_shipped_dataset_has_no_hard_findings` now allow-lists precisely this one `(SEK1.M, zahlen.json, size-target-exceeded)` triple and fails on any other soft finding. Supersedes the E12-16 acceptance criterion "every part inside the §6.7 target". |
| 2026-08-03 | NOR40271469 (Volksschule), PRIM.M section `Kompetenzbeschreibungen, Lehrstoff (1. bis 4. Schulstufe):`, elements 5074–5285 (**V-45**, **V-70**) | PRIM.M carries `lehrstoff_quelle: eigen_ausgewiesen`, so a Lehrstoff text is stated in its own section; nothing extracts it, so the shard risked shipping a metadata axis pointing at content the dataset does not contain | **Measured: there is no separate Lehrstoff prose block.** The section spans 212 elements and contains *only* competence structure: 4 school-year headings, 16 `Kompetenzbereich` headings (4 areas × 4 years), 16 verbatim stem paragraphs, and 16 `liste`/`aufzaehlung` blocks holding **40 `listelem`** — which is exactly the **40 competence records PRIM.M ships**, a 1:1 correspondence with nothing left over. The only other content is the cross-cutting-themes legend table (1 `table`, 13 label paragraphs, 13 `super` markers), which is already parsed. Zero paragraphs under the heading are Lehrstoff prose distinct from a competence stem. The heading occurs twice in the document; the second (element 6984) belongs to *Bewegung und Sport*, a subject outside the six, so V-70's "PRIM.M is the only such heading" holds within scope. | **DECIDED + DONE — V-45 closes; no extraction path is needed.** PRIM.M's Lehrstoff *is* its competence descriptions; the regulation states them under a heading that names Lehrstoff instead of deriving them from Anwendungsbereiche it does not have. `lehrstoff_quelle: eigen_ausgewiesen` is therefore accurate as a **provenance** label — it says where the Lehrstoff lives, not that a further unextracted text exists — and the dataset contains 100% of what is under that heading. Rejected: building a PRIM.M-specific Lehrstoff extraction path (there is no distinct text for it to extract) and renaming the axis value (the discrimination V-70 measured is real and E4 dispatches on it). Consumers must read PRIM.M's Lehrstoff from the competence records, not from an absent dedicated field. |
| 2026-08-03 | E5-07 task premise vs. repo state: `at-differenzierung/references/example_lesson.json` | The task described this fixture as an existing file to be updated "in both skills" | The file does not exist and never has (`git log --all` on the path is empty); only `at-unterrichtsplanung` ships a committed fixture. `at-differenzierung/scripts/render_all.sh` only names the runtime input convention (`differentiation.json`), it is not a shipped example | Source (repo state) wins: created `plugin/skills/at-differenzierung/references/example_lesson.json` from scratch — a differentiation plan for the same 2. Klasse fraction-comparison topic as the `at-unterrichtsplanung` fixture, exercising `kompetenzbezug`, `uebergreifende_themen_tag`, `niveau_spalte` (3 tiers) and the new `herkunftsblock` (origin marking, E5-07), including `herkunftsblock` nested inside a `niveau_spalte` tier and `from_shared` nested inside `herkunftsblock`/`group`. This is also the first fixture anywhere in the repo to exercise `niveau_spalte` (E5-06 shipped with no fixture coverage). |
| 2026-08-03 | E5-07: origin marking design (no prior concept existed — zero hits for `herkunft`/`quelle_typ`/`amtlich`/`teacher_supplied` in the renderer before this task) | Backlog row only specifies "visually distinct" for official RIS vs. `docs/`-supplied content; no field shape given | Plan §6.6/§4 already fixes the vocabulary elsewhere (`herkunft: "docs"`, `amtlich: false` on the `Lernaufgabe`/`Fehlvorstellung` data shapes) | Added a new nesting block type `herkunftsblock` (`amtlich: bool`, `quelle` dict when official — reusing `kompetenz_citation()`'s NOR/Kundmachung/Stand format verbatim, no second citation format invented — or free-text `quelle_hinweis` when teacher-supplied, plus a `blocks` list of arbitrary nested content) to both `lesson_common.py`/`render_lesson_docx.py`/`render_lesson_html.py`/`theme.css` files (kept byte-identical across `at-unterrichtsplanung` and `at-differenzierung`). Threaded through all four recursive passes (`_repair_enum_breaks`, `_repair_pipe_tables`, `_repair_inline_bullets`, `expand_blocks`) the same way `niveau_spalte` was for E5-06, and verified nested `from_shared` and nested `herkunftsblock`-inside-`niveau_spalte` expand correctly by inspecting the re-opened `.docx` XML. `amtlich is True` (strict identity check) is the only value that renders as official; anything else — `False`, missing, `None`, a stray string — renders as teacher-supplied, so a missing/ambiguous flag can never default to claiming official status. Visual distinction: distinct icon (`§` reusing the existing kompetenz icon vs. `📁`), distinct label text, distinct CSS class/left-border accent color (`#7A8CC4` reused from `.co.kompetenz` vs. new `#B2652E`); docx carries the same left-border accent via `_cell_borders`. Existing fixtures with no `herkunftsblock` are unaffected — it is a new opt-in block type, nothing was made required. |
| 2026-08-03 | Plan §5's tool contract vs. the shipped six-shard directory layout (E4-01, `plugin/scripts/kompetenz.py`) | `finde_kompetenz(fach, ...)` takes a single `fach` argument; `finde_bildungsstandard_bezug(kompetenz_id)` returns `Deskriptor[]`; `finde_anwendungsbereiche(kompetenz_id)` needs no extra parameter beyond the plan's two | The shipped layout is `<band>/<fach>/` and no bare `fach` value (e.g. `"M"`) identifies one of the six shards — PRIM.M and SEK1.M share the same Fachcode. `plugin/data/bildungsstandards/` ships only `.gitkeep`; the E8 crosswalk that would produce real `Deskriptor` content does not exist yet. A resolved competence record already carries enough (`bereich_slug`, `stufe`) to look up `bereich`/`stufe`-bound application items via `meta.anwendungsbereiche_bloecke` (constraint 6), so the E4-04 backlog row's proposed `(fach, stufe, bereich)` alternate signature turned out unnecessary. | **DECIDED + DONE.** `finde_kompetenz`'s `fach` parameter is the `"<BAND>.<FACH>"` shard key already used everywhere else in this codebase (FINDINGS.md, the test suite, `index.json`'s own `meta.band`/`meta.fach.code`), e.g. `"SEK1.M"` — documented in the function's docstring, not a silent overload. `finde_bildungsstandard_bezug` returns an honest `{"abgedeckt": True, "deskriptoren": [], "hinweis": "Bildungsstandards-Crosswalk noch nicht erstellt (E8 offen)"}` for the five `verordnet` shards instead of fabricating crosswalk content; PRIM.SU's `{"abgedeckt": False, "grund": "keine BiSt verordnet"}` is unaffected and unchanged. `finde_anwendungsbereiche` keeps the plan's exact two-parameter signature (`kompetenz_id`, `nur_verbindlich`) and resolves `bereich`/`stufe`-bound items via the record's own `bereich_slug`/`stufe`, so no alternate signature was added. |
| 2026-08-03 | V-71 (`stichwort_index` is exact-token) vs. `finde_kompetenz(..., stichworte=...)`'s routing (E4-01/E4-02) | A "fall back to substring matching only when the exact key is absent" strategy would resolve V-71 | Measured against the shipped SEK1.M index: `"bruch"` **is** an exact key (routes to `daten.json`, `zahlen.json`) and `"bruchtermen"` is a *separate* exact key (routes to `variablen.json` only) at the same time — an exact hit for the root term does not preclude an unrelated compound existing elsewhere. A fallback-only-on-miss strategy would still miss `variablen.json` for a `"Bruch"` search, because the `"bruch"` lookup never misses in the first place. | **DECIDED + DONE.** `_stichwort_dateien` always unions the exact-key hit (when present) with every index key containing the term as a substring, never conditionally on a miss. The routing step is deliberately generous (recall-favouring); `finde_kompetenz` re-checks each candidate competence's own `stammsatz`/`text`/`text_roh` afterwards, so the final result list stays precise even though routing over-selects files. Verified against the shipped data: searching `"Bruch"` in SEK1.M now routes to `daten.json` + `zahlen.json` + `variablen.json` (`test_finde_kompetenz_stichworte_bruch_routes_through_compound_fallback`, `data-pipeline/tests/test_kompetenz_access.py`). |
| 2026-08-03 | Plan §6.6's `docs/` ingestion contract vs. E4-01's `finde_lernaufgaben` scope | BACKLOG's E4-07 depends on E6-05 (the full ingestion pipeline: alias table, `.md`/`.txt` native, `.pdf`/`.docx` conversion, `docs/.cache/`, size/count/token limits); E6-05 is not built | E4-01's own task text asks for all nine `finde_*` functions to be callable now, including `finde_lernaufgaben`, with only "missing/empty `docs/` returns `[]`" as its explicit acceptance | **DECIDED + DONE.** Shipped a minimal, interim `finde_lernaufgaben`: native `.md`/`.txt` only, the folder convention (`docs/<fach>/<stufe>/…` \| `docs/<fach>/…` \| `docs/…` unassigned) and the fach alias table, but no PDF/DOCX conversion, no `docs/.cache/`, and none of the size/count/token limits — E6-05 must still add those, including logging an unconvertible file as "unusable" rather than silently skipping it. Every result carries `herkunft: "docs"` / `amtlich: False`. `docs/README.md` (the folder's own scaffolding, the one non-`.gitkeep` file the repo commits there) is explicitly excluded so it never surfaces as if it were a teacher's Lernaufgabe. |
| 2026-08-03 | Plan §9/§10 and E5-08 require a deterministic `lesson.json` → expected DOCX golden-file comparison | Raw DOCX archive bytes were expected to be stable enough for a direct file comparison | Re-rendering both skill fixtures changes the ZIP member timestamps, so all four raw `.docx` files differ between runs. The package payload is stable: each output has the same **17 uniquely named members**, and every member's bytes — including `docProps/core.xml`, OOXML and relationships — match across repeated renders. The four ordinary compressed goldens total **164 KB**; storing canonical uncompressed archives would inflate them to about 3.4 MB without testing more content. | **DECIDED + DONE (V-74).** Golden tests compare the complete `{member name: member bytes}` map against a checked-in expected DOCX and against a second render. ZIP timestamps, compression metadata and member ordering are intentionally outside the contract; any package-member addition, removal or byte change fails. Both fixtures and all four Austrian block types are pinned, and the paired renderer sources must remain byte-identical. `python-docx` remains optional: the DOCX module skips cleanly when it is absent. The repository-wide pytest command, not the old `data-pipeline/tests`-only command, is now the authoritative baseline. |
| 2026-08-03 | E4-02 / plan Phase 1 expects `finde_kompetenz(... stufe="K2", stichworte=["Bruch"])` to resolve through `index.json` plus one part and return official source data | The shipped index and the already-decided V-71 compound-recall rule cannot satisfy the one-part projection. Exact key `bruch` names `daten.json,zahlen.json`; substring expansion must also add `variablen.json` because it contains `bruchtermen`. Measured access: index + those **3 candidate parts**, excluding `figuren.json` and `zusatz.json`; result IDs are `AT.LP23.SEK1.M.ZAHLEN.K2.02` and `.03`. Public competence results previously omitted the part-level RIS provenance, so “with source” was not literally met. | **DECIDED + DONE (V-75; closes V-73).** Preserve V-71 recall and supersede “one part” with “only index-selected candidate parts, never the whole shard.” Every returned competence now receives a copy of its part's official `provenienz`. `stichwort_abdeckung` remains outside the nine-function contract and additively reports candidate files, matching competence IDs, matching `art: praezisierung` Lehrstoff items, a structured status and an explicit non-absence hint. Measured item-only cases: `Bruchtermen` → non-binding `...AB.VARIABLEN.K4.03`; `Zinseszinsen` → binding `...AB.VARIABLEN.K3.21`; each reads only index + `variablen.json`. No index candidate is explicitly “unknown”, never “absent from the curriculum”. Shipped data and the build-time index are unchanged. |
| 2026-08-03 | E4-03 backlog shorthand says progression is “textual for SEK1.M, positional elsewhere” | `Wiederholen und Festigen:` is a real source-text trigger in SEK1.M, but the regulation supplies no target IDs. `link_wiederholungen` therefore assigns that item's `wiederholung_von` **positionally** to every competence in the same area and previous year, then independently mirrors the same adjacent-year area grid into every competence's `vorlaeufer`/`folge`. The public competence graph is positional for all six shards; SEK1.M additionally carries 16 text-triggered item backlinks that agree with it. Measured public graph: **968 directed links** over 247 records, 0 asymmetric, 0 cross-area, 0 non-adjacent-year, 0 missing `volltext`/`provenienz`. | **CLARIFIED + DONE (V-76).** Preserve the existing data and access implementation. Tests now pin one source-triggered SEK1.M case (`AB.ZAHLEN.K2.05` → competence `ZAHLEN.K2.02`, whose three K1 predecessors exactly equal the item's `wiederholung_von`) and one purely positional SEK1.D case (`LESEN.K1.01` ↔ `LESEN.K2.01`), both directions with full quotation and RIS source. “Textual for SEK1.M” means the item backlink is triggered by official text, not that the source identifies its targets or that the competence graph uses a different algorithm. No shipped bytes changed. |
| 2026-08-03 | E4-04 access audit against the shipped SEK1.D `bereich` blocks, especially the source-declared structural area `SPRACHREFLEXION` (**V-77**) | The E4-01 decision above said a resolved competence always carries enough area/year context, so the plan's competence-ID-only signature could reach every application block and the proposed `(fach, stufe, bereich)` alternative was unnecessary | SEK1.D ships **16** area/year blocks with **54** unique official items. Competence-ID lookup truthfully reaches 12 blocks / 42 unique items; the remaining four `SPRACHREFLEXION.K1..K4` blocks contain **12** items but the source explicitly gives that structural area **zero competence records**, so no competence ID can address them. Attaching them to a neighbouring competence would invent attribution. The same audit confirms SEK1.M exposes 198 competence-attached precisifications (166 binding, 32 `allenfalls`; the other 39 of the 237 section items are digital-technology suggestions), and every coarse-bound item retains `kompetenz_id: null`. | **SUPERSEDES the E4-01 “alternate signature unnecessary” decision; DECIDED + DONE.** Both access functions keep every existing positional competence-ID call and add a keyword-only coordinate mode. `bindung: bereich` requires `fach` + `stufe` + `bereich` and reads the canonical slug/name block directly; `bindung: stufe` requires only `fach` + `stufe` and rejects a false area filter; `kompetenz` and PRIM.M `eigen_ausgewiesen` remain competence-specific; `prosa`/`keine` stay defined-empty. The 12 structural items are now reachable without changing their null `kompetenz_id`; all 54 SEK1.D items are covered. Shipped data and frozen bytes are unchanged. |
| 2026-08-03 | E4-06 public differentiation behavior vs. the already-shipped `meta.differenzierungs_achse` boundary and E4-03 progression contract (**V-78**) | Returning the metadata axis was assumed sufficient to enforce `gilt_ab_stufe: "K2"`, and `vorklasse_stuetzen` preferred a current-year `Wiederholen und Festigen` item whenever one existed before falling back to predecessor competences | Two access defects were measured. (1) All **28** K1 competence queries across SEK1.M/D/E exposed top-level `niveaus: ["Standard", "Standard AHS"]`, even though the source says the distinction begins at K2. (2) For **16** SEK1.M competences (5 K2, 6 K3, 5 K4), `vorklasse_stuetzen` returned a raw current-year application item instead of the positionally resolved prior-year competences; those 16 results lacked both `volltext` and `provenienz` and disagreed with `finde_progression(..., "zurueck")`. The remaining five shards already matched progression. | **DECIDED + DONE.** `achse` remains the verbatim metadata object, but top-level `niveaus` is now the effective label list: empty before `gilt_ab_stufe`, populated at/after it; primary generic labels apply throughout. `vorklasse_stuetzen` now always uses the single E4-03 predecessor graph. The official Wiederholen items remain available via `finde_anwendungsbereiche` as evidence, never mixed into a competence-result list. Measured after: **484** backward supports (77/84/72/81/100/70 by PRIM.M/PRIM.D/PRIM.SU/SEK1.M/SEK1.D/SEK1.E), all with full quotation and RIS provenance; enrichment remains exactly **32** real `allenfalls` items in SEK1.M and zero elsewhere. No shipped bytes changed. |
| 2026-08-04 | Plan §6.1 subject routing vs. the six shipped shards, while building `at-unterrichtsplanung` (E6-01/E6-02) | §6.1 defines five routing groups, including `Society` (History/civics, geography/economics) and `Science & Sachunterricht` (Sachunterricht plus Bio/Physics/Chemistry) | Scope is fixed at exactly six shards (`PRIM.D`, `PRIM.M`, `PRIM.SU`, `SEK1.D`, `SEK1.E`, `SEK1.M`). **No shard exists for the `Society` group at all**, and `nawi` resolves to `PRIM.SU` alone. §6.1 itself marks both sets of subjects "(later)", so this is a gap the plan anticipated rather than a source contradiction. Measured against the shipped part files: `PRIM.SU` carries society content across **four of its six** Kompetenzbereiche — `SOZIALWISS`, `HISTORISCH`, `GEOGRAFIE`, `WIRTSCHAFT`. | **DECIDED + DONE.** `references/gesellschaft.md` ships as an explicit placeholder that (a) states plainly that no amtlicher RIS competence dataset exists for those subjects, (b) **forbids inventing a Kompetenz-ID, a NOR citation or a Bildungsstandard reference** for them, (c) routes genuine primary-band society requests to `nawi.md`/`PRIM.SU`, and (d) otherwise offers the teacher an unanchored unit that must **not** be labelled kompetenzorientiert or amtlich verankert. Rejected: omitting the file entirely (loses the refusal path and invites fabrication for a group the frontmatter advertises); deferring the group until a shard exists (leaves routing silent). The file is a placeholder by design and anticipates no data it does not have. |
| 2026-08-04 | `finde_anwendungsbereiche` / `finde_lehrstoff`, legacy competence-ID path vs. the E4-04 coordinate path, surfaced by the E4-08 contract fixture (**V-79**) | The two call paths were expected to agree on how an absent `(bereich, stufe)` block is reported | They do not, and the divergence is deliberate rather than a defect. Reaching an absent block **via a competence ID returns `[]` silently**; reaching it **via explicit `fach`/`stufe`/`bereich` coordinates raises `ValueError`**, because coordinates are an explicit claim that the block exists. Verified on shipped data: `finde_anwendungsbereiche(fach='SEK1.D', stufe='K1', bereich='GIBTESNICHT')` → `ValueError: unbekannter bereich 'GIBTESNICHT' fuer 'SEK1.D'`. **Reachability measured across all 247 shipped competences (orchestrator, correcting the subagent report by omission): 79 return empty via the ID path, but 77 of those are the defined-empty axes `keine` (PRIM.M, 40/40) and `prosa` (SEK1.E, 37/37), and 2 are SEK1.M competences with no attached items. All three block-bound shards return 0 empties — `PRIM.D` 0/40, `PRIM.SU` 0/48, `SEK1.D` 0/40.** The missing-block branch therefore has **zero occurrences in shipped data** and is exercised only by the synthetic mini fixture. | **DECIDED + DONE.** The asymmetry is pinned as intended behaviour by two named tests in `data-pipeline/tests/test_kompetenz_contract.py`, so a future B2/SQLite implementer preserves it deliberately instead of accidentally normalising both paths. Recorded here because it is invisible in shipped data and would otherwise be re-derived as a bug. Rejected: making both paths raise (breaks the legacy ID contract, which must stay defined-empty); making both return `[]` (silently swallows a caller's false claim that a block exists). |
| 2026-08-04 | BiSt Anl. 1 `NOR40255561` (81,009 bytes), parsed for the first time (E8-01/E8-02, **V-80**) | The plan's "393 elements" was taken as approximate, and the document was expected to follow the Lehrplan's element conventions | **393 is exact** — verified by the orchestrator: `nutzdaten/abschnitt` has exactly 393 children. Four shapes have **no Lehrplan analogue**: (1) a new element `<gdash/>`, 8 occurrences all in M8, which naive `itertext()` drops silently, turning `(Un-)Gleichungen` into `(Un)Gleichungen`; (2) two "fused `<schlussteil>`" cases (D4 Rechtschreiben, D8 Lesen) where Titel+stem sit *inside* `<liste>` rather than a preceding `<absatz>`; (3) two standalone single-sentence competences (M4 Modellieren/Problemlösen) carried in `<absatz>` with no `<liste>` at all; (4) D8's four areas each carry a general description sentence before the first titled sub-group. The `typ` attribute is **inconsistent between occurrences** — one Abschnitt boundary is `g1min` and later ones `g1`; one subject name is `typ="para"`, another `typ="g2"`. M8's model is two-dimensional: 4 Handlungsbereiche × 4 Inhaltsbereiche = **16** combined areas. Two M8 headings carry typesetting glitches (one keeps a colon the other 15 drop; one lacks its closing guillemet). | **DECIDED + DONE.** The parser matches on heading **text**, never `typ`. `<gdash/>` renders as a literal hyphen, pinned by a dedicated test — orchestrator-verified: `(Un-)Gleichungen` appears verbatim in the shipped `m8.json`. Area descriptions are captured verbatim on `Kompetenzbereich.beschreibung`; an early parser draft silently dropped them as "unexpected", which is the **fourth** independent silent-loss mechanism this project has found, so the standing risk row stays un-retired. Heading glitches are preserved verbatim in `ueberschrift_roh` and kept out of the code-minting path. Measured and orchestrator-confirmed: **38 areas / 268 descriptors** — D4 5/75, M4 8/58, D8 4/52, E8 5/35, M8 16/48 — with 0 empty texts. |
| 2026-08-04 | BiSt descriptor identifiers, alongside the frozen `AT.LP23` grammar (E8-02) | The BiSt shards might reuse `id_schema.AREA_CODES` and the Lehrplan ID grammar | BiSt is a different regulation with a different shape: single-grade checkpoints (4./8. Schulstufe), not a four-year grid, so the Lehrplan's `<Bereich>.<Stufe>.<lfd>` tail does not fit. Reusing the frozen grammar would have meant either bending it or minting Lehrplan-looking IDs for non-Lehrplan content. | **DECIDED + DONE.** A separate namespace `AT.BIST.<Fach>.<Programmstufe>.<Bereich>.<lfd>` (6 segments; `Fach`=D\|M\|E, `Programmstufe`=SCH4\|SCH8, `lfd` scoped per (Fach, Programmstufe, Bereich)) in a new `schema/bist_id_schema.py` with its **own** `BEREICH_CODES` table. The frozen `AT.LP23` grammar and `id_schema.AREA_CODES` are untouched. Collision is structurally impossible — the second segment differs. Orchestrator-verified: **268 IDs, 268 unique, all `AT.BIST`, zero `AT.LP23` prefixes.** |
| 2026-08-04 | The six rubric CSVs, re-measured before the E10-08a language pass | The backlog's 2026-07-30 snapshot recorded 87/101 defective rows with per-term counts (`Lektion` ×22, `Gerüst` ×16, `Klassenstufe` ×8, `Curriculum` ×6) | The snapshot **undercounted every term**. Re-measured: `Lektion` **34**, `Gerüst` **23** (the earlier count missed the verb form `gerüstet`), `Klassenstufe` **15** (incl. a lowercase `klassenstufe-gerecht`), `Curriculum` **16**, `Pass =`/`Fail =` 52/68. Rows carrying ≥1 named defect: **84/101**, not 87 — the row total was slightly over while every individual term was well under. Three further wrong-or-inconsistent words for one artifact were found that the snapshot did not name at all: `Stundenplan`/`Lehrerplan`/`Stundenplanung` (15+14+1). | **DECIDED + DONE.** Terminology chosen from the product's own vocabulary rather than invented: scaffold → `Stützung` (already used correctly in 3 untouched `shared.csv` rows, so the other 19 were conformed to it); `Lektion` → `Unterrichtsstunde`; `Klassenstufe` → `Schulstufe`; `Pass =`/`Fail =` → `Bestanden =`/`Nicht bestanden =` (matches `README.md`'s own gloss); the three plan-document words unified to `Stundenverlauf`, which is what `SKILL.md` already calls it; `Curriculum` disambiguated into `Lehrplan` (official) vs `Lehrwerk` (a named commercial program), which the CSVs had conflated — actively misleading in Austria. 93 of 101 rows changed; the 8 untouched were each confirmed defect-free rather than skipped. **Deliberately out of scope:** ~73 `failen`/`gelabelt`-style Denglish verbs, a pervasive register choice rather than a targeted defect. Orchestrator-verified: row count still **101** (42/7/8/15/28/1), header and 6-column shape intact, **row IDs byte-identical** in all six files, and the one surviving `Curriculum` is the row identifier `M-CLARIFY-CURRICULUM`, correctly left alone. |
| 2026-08-04 | E10-08a acceptance, whose bar is human judgement: "a native Austrian German speaker would not flag any rubric row as awkward or wrong" | Mechanical verification (defect classes at zero, 101 rows, IDs byte-identical, all six files parsing) can show the pass was *executed*, but cannot satisfy an acceptance criterion that is explicitly about how the German *reads* | The 93 changed rows were classified against the seven terminology rules: **74 changed by nothing but mechanical substitution**, leaving **19 with substantive rewriting**. This reduced the human review surface from 93 rows to 7 decisions plus 19 rows, presented with word-level before/after marking. | **CONFIRMED BY THE PROJECT OWNER 2026-08-04.** All seven terminology decisions accepted (`Lektion`→`Unterrichtsstunde`, scaffold→`Stützung`, `Klassenstufe`→`Schulstufe`, `Curriculum`→`Lehrplan`/`Lehrwerk`, `Pass`/`Fail`→`Bestanden`/`Nicht bestanden`, the three plan-document words→`Stundenverlauf`, `Sinnmachung`→`Sinnstiftung`), and all 19 substantively rewritten rows read as sound. **The acceptance criterion is therefore met by the human judgement it requires, not merely by mechanical evidence** — same pattern as the E12-16 owner confirmation of the 608-byte SEK1.M overage. Also accepted: leaving the ~73 `failen`/`gelabelt`-style Denglish verbs untouched as an established register choice rather than a targeted defect; reversing that remains a separate, larger decision and is **not** part of E10-08a. |
| 2026-08-04 | Plan §6.2's full single-turn document set vs. the E6-03 task boundary | §6.2 names `unterrichtsplanung`, optional `schueler_material` and `beobachtungsbogen` in the same planning flow | E6-03's explicit acceptance is the narrower first executable path: `lesson.json` → DOCX in one turn for Sek I mathematics. E6-06 separately owns rendering the complete document set, while E6-04 owns every-plan anchoring enforcement and E6-05 owns full `docs/` ingestion. | **DECIDED + DONE for E6-03.** The flow now routes, asks at most two targeted questions while offering an immediately usable proposal, anchors only through `kompetenz.py`, activates a real predecessor via the Spiralprinzip, and renders at least `unterrichtsplanung` in the same turn. The skill explicitly does **not** claim the later three tasks are complete. A real K2 mathematics fixture proves one-command DOCX+HTML rendering; E6-04/E6-05/E6-06 remain open rather than being silently folded into this task. |
| 2026-08-04 | Plan §7.4/V-43 area-crosswalk projection vs. the five shipped BiSt shards (E8-03, **V-82**) | Mathematics was projected as a near-direct mapping from four BiSt content areas to four Lehrplan areas; one strategy was expected to cover M4 and M8 | Measured crosswalk shape is **50 assignments**, covering all 20 official non-SU Lehrplan areas and all 38 BiSt areas. M4 needs four content correspondences **plus 16 transversal process relations** (four general mathematical areas apply across each of four Lehrplan content areas). M8 is a 4 Handlungsbereich × 4 Inhaltsbereich matrix and needs **16** relations by shared Inhaltsachse. D4/D8/E8 add 14 further assignments. The two synthetic `GZINTEGRATIV` records have no official Lehrplan area counterpart; PRIM.SU has no BiSt chapter. | **DECIDED + DONE.** Ship a project-authored `crosswalk.json` with a written rationale and `amtlich:false` on every assignment. Its method states explicitly: “Bereichsbezug; keine 1:1-Zuordnung von Lehrplan-Kompetenzen zu Bildungsstandard-Deskriptoren.” Public access returns only descriptors from mapped target areas, deduplicated, with official endpoint provenance kept distinct from the non-official rationale. PRIM.SU stays exactly defined-empty; GZ stays covered-but-unmapped. The SEK1.D `SPRACHREFLEXION`→D8 `SPRACHBEWUSSTSEIN` mapping ships, but its 12 descriptors are not competence-ID reachable because the source area has zero competence records (V-77). Existing official shard bytes are unchanged. |
| 2026-08-04 | E10-08b/V-81 rubric framework audit against the six shipped Lehrplan shards (**V-83**) | V-81 states a 27-row substitution scope but explicitly enumerates NGSS 7 + C3 7 + Common Core 4 + WIDA 1 | The enumeration sums to **19**. A full substantive audit found the missing eight: `shared.csv` P2/P3/O5, `differenzierung.csv` P4/O5/M2, and `deutsch.csv` P-D4/P-D5. Those rows require foreign standard codes, UbD/UDL/proficiency/IEP framing, or US literacy-policy constructs not contained in the shipped Austrian regulation data. | **DECIDED + DONE.** Rewrote exactly 27 rows against shipped Austrian constructs. NGSS rows now follow PRIM.SU areas/operators; C3 rows follow the four society-related PRIM.SU areas, source-specific operators, stage-bound Anwendungsbereiche and `keine_verordnung`; Common Core rows follow PRIM.D/SEK1.D progression and source requirements; WIDA uses the shipped Standard/Standard-AHS and GERS metadata. The eight audit-derived rows use AT.LP23 progression, competence-derived Lernziele/access aids, actual differentiation axes and PRIM.D Schriftspracherwerb. CSV schema, 101-row total, row IDs and order are preserved; four applicability conditions were corrected to source-backed Austrian scopes. |
| 2026-08-05 | Verification of the local-only `REVIEW_FINDINGS.md` review of `0604b5e`/`ad8346c`/`6084747`, re-measured against the shipped files and the public access layer (**V-84**) | Seven review findings, each stated as a defect in shipped behaviour and each expected to be recordable as written | **Five hold; two rest on a false premise.** Confirmed exactly: R-01 (13 of 14 Sachunterricht domain rows put their scope in `Notes` while `Conditional` — the column `evals/README.md` designates — is empty), R-03.2 and R-03.3 (both quotations match the shipped text verbatim). Confirmed but understated: R-02 (three distinct BiSt result shapes exist, and the methodological `hinweis` is present on **successful** mapped results, so `SKILL.md:118` misfires on the success path); R-03.1 (60 is exact, and a **61st** non-first-stage record, `AT.LP23.SEK1.M.GZINTEGRATIV.K3.01`, also has an empty `vorlaeufer`); R-05 (`nicht_abgedeckt.lehrplan_fach` is a bare string, not even the five-value enum). **Premise wrong:** R-04 — `P-S3`/`P-S5` already carry the operator guards the review says are missing, so only two narrow overreaches remain; R-06 — the theme query is **not** empty, it returns `['Sprachliche Bildung und Lesen']`, so the fixture contradicts the API rather than inventing into a void. R-07 is a commit-history claim and was **not** verified. | **DECIDED + DONE (recording only; no code change in this row).** Recorded as V-84 with the two false premises corrected rather than as the review states them, because recording R-04 and R-06 verbatim would have produced wrong fixes — R-04 would have stripped correct operator guards, R-06 would have omitted a theme instead of correcting it. Remediation split into **E10-08c** (R-01, highest priority — it invalidates evaluation results), **E10-08d** (R-03 plus R-04's narrowed residue), **E6-07** (R-02, to land before or with E6-04), **E6-08** (R-06) and **E8-06** (R-05, regression guard only — the shipped 50 assignments are correct). GATE G4's "rubrics pass" amended to require E10-08c/E10-08d. Two risk rows added: unenforced rubric applicability, and treating a review as truth without measurement. Measurement traps recorded: the predecessor field is `vorlaeufer` (not `vorgaenger`/`voraussetzung`, which do not exist), and a raw-JSON sweep counts 68 first-stage records against the public API's 60 because SEK1.M repeats IDs across stages. |
| 2026-08-05 | Scoping the rubric-applicability fix; searched for whatever consumes `evals/` (**V-85**) | The rubric `Conditional` column is enforced by some runner, so a fix can be proven by a test that a science plan skips society-only criteria — as the review recommended and as the task was originally filed | **No code reads `evals/` at all** — no Python, no shell, no workflow, and `.github/` does not exist (E10-02..05 all still open). `evals/README.md` line 60 states the rubrics are for *“manuelle oder periodische Bewertung … nicht für automatische Durchläufe bei jedem Commit”*. `Conditional` is honoured by a human or model reader. There is therefore no skipping behaviour to assert, and the filed acceptance criterion was unwritable. | **DECIDED + DONE (criterion corrected; task not yet implemented).** E10-08c's acceptance replaced with the checkable data invariant: *no row states a domain scope in `Notes` that its `Conditional` does not encode*, across all six CSVs — which is also the exact condition that retires the rubric-applicability risk row. Explicitly **out of scope:** building an eval runner to satisfy the original wording. Recorded that rubric quality is un-instrumented: every rubric defect to date was found by a human or a review reading the rows, never by a check, so "the rubrics pass" stays a human judgement until a runner exists. |
| 2026-08-05 | `M2` (`differenzierung.csv`) no-predecessor fallback, owner decision for E10-08d | The rewritten `M2` requires the model, absent learner information, to explain that it will use the shipped predecessor competences as support | **61 records legitimately have no predecessor** — the 60 first-stage `SCH1`/`K1` records plus `AT.LP23.SEK1.M.GZINTEGRATIV.K3.01` — so the promised fallback is unsatisfiable for them and the row grades against something the data cannot provide. | **DECIDED (owner-confirmed 2026-08-05; implementation tracked as E10-08d).** Where no predecessor exists, `M2` requires the model to state plainly that the target competence has no predecessor and to name what it uses instead — the `stammsatz` and the binding Anwendungsbereiche. It may not promise a predecessor it cannot deliver, and may not silently drop the transparency requirement. The branch keys on an **empty `vorlaeufer` array**, not on an `SCH1`/`K1` ID test, so the non-first-stage GZ record is covered. |
| 2026-08-05 | E6-07: the planning skill's own documentation of `finde_bildungsstandard_bezug`, checked against the runtime (**V-84** R-02) | `SKILL.md` states that empty descriptors **or any `hinweis`** mean the crosswalk is outstanding, and `references/kompetenzdaten.md` states the five regulated shards return `{"abgedeckt": True, "deskriptoren": [], "hinweis": …}` because the table "ist noch nicht gebaut" | Both are false since E8-03. **Three** distinct shapes exist, not two, and the methodological `hinweis` is present on every **successful** mapped result — so the documented rule misfires precisely on the success path and could tell teachers a completed crosswalk is pending. Orchestrator-verified key sets: mapped and `GZINTEGRATIV` both return `{abgedeckt, deskriptoren, hinweis, methodik, zuordnungen}`; `PRIM.SU` returns only `{abgedeckt, grund}` — no `deskriptoren`, no `hinweis`. | **DECIDED + DONE.** Both files now document the three shapes distinctly, naming which field is present in each and stating explicitly that the mapped-case `hinweis` is the methodological precision statement and **not** a pending-crosswalk signal. Both `hinweis` strings are quoted verbatim from the runtime; the `praezisionsaussage` is unchanged; the "never invent a descriptor" prohibition is retained in all three branches. `methodik` — a real returned field absent from the task brief — is documented too. `kompetenzdaten.md` grew 1,709 → **1,964** tokens against the ~2k §6.7 soft target, leaving ~36 tokens of headroom: **the next addition to that file triggers a sharding review.** `SKILL.md` (2,912 tokens) is not under that budget, which §6.7 scopes to `references/<group>.md`. Documentation only; no code or data changed. |
| 2026-08-05 | E8-06: the crosswalk schema's ability to catch a cross-subject swap (**V-84** R-05) | `lehrplan_fach` and `bildungsstandard_shard` are validated as two independent enums with no `allOf`/`if-then`/`oneOf`/`anyOf` anywhere, and the semantic tests derive expectations from the crosswalk itself, so a swap such as `PRIM.D→D8` together with `SEK1.D→D4` preserves every count and coverage figure while staying schema-valid and test-green. `nicht_abgedeckt.lehrplan_fach` was a bare `{"type":"string"}` | Confirmed as a regression-guard gap; **the shipped 50 assignments are correct**, so no data error existed. Measured after the fix: all **20** wrong fach→shard combinations are rejected; the 9-row `D4↔D8` swap is rejected with 9 errors while `len(zuordnungen)` stays 50; a missing `bildungsstandard_shard` is caught. | **DECIDED + DONE.** Five `if/then` pairs under `allOf` on `$defs/zuordnung`, plus `nicht_abgedeckt.lehrplan_fach` → `enum:["PRIM.SU"]`. No data touched; `shipped_shards.sha256` still matches and the validator is unchanged at 0 hard / 1 accepted soft. **The guard depends on a precondition that is easy to remove by accident:** the `then` clauses carry no `required`, so they hold only while `bildungsstandard_shard` remains in the zuordnung-level `required` list — drop it and every `if` misses and the rule silently passes. A dedicated test now pins that. **Orchestrator rewrote the subagent's tests** rather than recording them as delivered: they validated `$defs/zuordnung` in isolation instead of the document path, one case (`SEK1.E→E4`) was vacuous because `E4` fails the enum regardless and would have passed before the fix, the identifier `…abstoßen…` was the only non-ASCII identifier in the repository (§6.8 requires ASCII identifiers), and `lehrt` was wrong German for "erzwingt". Verified by mutation: stripping the guard fails exactly the three tests that assert it. |
| 2026-08-05 | E10-08c: rubric applicability scope in `evals/at-unterrichtsplanung/rubrics/sachunterricht.csv` (**V-84** R-01, **V-85**) | `evals/README.md` designates `Conditional` as the column that makes a criterion skippable, so the science/society domain scopes are expected to live there | 13 of the 14 domain rows leave `Conditional` empty and state their scope only in `Notes`, which is never consulted when skipping; `R-S2` encodes its quantitative-data condition but not its domain. A society plan is therefore graded by science-only criteria and vice versa. | **DECIDED + DONE.** Two slugs added — `PRIM.SU-Naturwissenschaft` (6 rows) and `PRIM.SU-Gesellschaft` (7 rows) — with `R-S2` carrying both its domain and its existing condition. Slug vocabulary taken from the rubric file itself (every affected `Criterion` ends in *(Naturwissenschaft)* or *(Gesellschaft)*) rather than from `id_schema.AREA_CODES`, to avoid importing another subsystem's naming into a human-read document. New `tests/test_rubric_conditions.py` (8 tests) asserts the invariant across all six CSVs keyed on `Notes` **wording**, not row IDs; mutation-verified by blanking the column. Structure orchestrator-verified: 101 rows, six columns, ID sequence byte-identical, only the `Conditional` column differs. **`; ` as a multi-condition separator was a new convention with no precedent and no documentation** — `evals/README.md` now defines it as conjunctive and states explicitly that scope belongs in `Conditional` because `Notes` is not evaluated when skipping. **Known limit, recorded rather than glossed:** the check enumerates five known scope phrasings, so a *novel* scope wording remains uncaught; the rubric-applicability risk row is **reduced, not retired**. |
| 2026-08-05 | E6-08: the planning fixture's theme tag and learning arc (**V-84** R-06) | Per V-84 as first recorded, the fixture's target competence returns `['Sprachliche Bildung und Lesen']`, so the `Informatische Bildung` tag should be *replaced* with the returned theme | **V-84 cited the wrong competence — the source wins over the tracking document.** The fixture anchors `AT.LP23.SEK1.M.ZAHLEN.K2.02` (*“nichtnegative Dezimal- und Bruchzahlen sowie ganze Zahlen …”*), not `K2.01` (*“mit Vielfachen und Teilern natürlicher Zahlen …”*). For `K2.02` the theme query returns `[]`, so the original review's premise ("the query is empty") was right and the orchestrator's correction of it was wrong. Root cause: competence IDs appear inside prose text rather than as `id` keys, an ID grep returned nothing, and the anchor was assumed from the fixture *filename*. **Both** competences return 4 Zuordnungen / 12 Deskriptoren, so the BiSt cross-check could not expose the mix-up. | **DECIDED + DONE.** Tag **omitted**, not substituted — the only option the source supports for an empty query; attaching a sibling competence's theme would have invented attribution. Integer arc reconciled by **adding** real integer work rather than narrowing the claim, because `finde_anwendungsbereiche(K2.02, nur_verbindlich=True)` returns 7 items including *“Vergleichen und Ordnen ganzer Zahlen”* and *“Darstellen ganzer Zahlen als Punkte auf der Zahlengeraden”* — narrowing would have dropped **binding** content. 7 binding / 0 optional confirms the fixture's "keine optionale Präzisierung" claim is source-true. `test_planning_flow.py` 3 → 6 tests, now asserting against live access-layer results instead of label/ID occurrence; drift-verified by reintroducing four defects. V-84's R-06 bullet has been corrected in place with the error and its cause recorded. **Standing lesson: read a fixture's anchor out of `kompetenzbezug`; never infer it from a filename.** |
| 2026-08-05 | E10-08d: five rubric rows checked against the shipped competence and descriptor text (**V-84** R-03/R-04) | Per the review, three rows misquote the source and two Sachunterricht rows retain wholesale imported-framework obligations | Three misquotes confirmed verbatim: `P-D1` attaches *mit Hilfestellung* to the wrong clause of `AT.LP23.PRIM.D.LESEN.SCH2.02`; `P-D6` demands `Quellenbezug` on `AT.BIST.D.SCH8.SCHREIBEN.07`, which has no source clause (that belongs to `AT.LP23.SEK1.D.SCHREIBEN.K4.02`); `M2` promises a predecessor fallback that **61** records cannot satisfy. **The R-04 framing was wrong and was not followed** — `P-S3`/`P-S5` already carried correct operator guards; only two narrow overreaches existed: an ordering absent from `PRIM.SU.TECHNIK.SCH2.01`, and a model check unconditional on model use where `PRIM.SU.TECHNIK.SCH3.02` binds it to itself (verified as the only PRIM.SU competence naming a Modell). | **DECIDED + DONE.** Five `What pass requires` cells rewritten, each bounded by its cited construct; the `P-S3`/`P-S5` operator guards were **preserved**, not rewritten. `M2` implements the owner decision of 2026-08-05, branching on a non-empty vs empty `vorlaeufer` array rather than an `SCH1`/`K1` ID test, so the non-first-stage `GZINTEGRATIV.K3.01` record is covered; the fail condition now also catches promising a predecessor that does not exist. Orchestrator-verified: exactly 5 rows differ and only in `What pass requires`; 101 rows, six columns, IDs, order and the `Conditional` column unchanged. **A silent-disable hazard was flagged in the brief and avoided:** `tests/test_rubric_conditions.py` keys on `Notes` wording, so rewording `Notes` would have made the invariant pass vacuously — no `Notes` cell was changed and `SCOPE_RULES` still matches 18 rows. Suite 1000 passed / 3 skipped. |
| 2026-08-05 | E6-04: making competence anchoring an invariant rather than an example | E6-03 specifies the anchoring flow in `SKILL.md` and demonstrates it once in the fixture, so E6-04 was expected to be a prose-strengthening task | **Prose cannot enforce it.** There is no `lesson.json` schema and the renderers accept whatever they are given, so a plan with a paraphrased quote, an altered `provenienz`, an invented "binding" item or a merged binding/optional section renders perfectly and no check anywhere notices. Enforcement therefore has to be executable and has to compare against the access layer, not against the document's own shape. | **DECIDED + DONE.** New `plugin/scripts/pruefe_verankerung.py` — pure stdlib, offline, deterministic, placed beside `kompetenz.py` so `at-differenzierung` (E7) can reuse it — with nine stable rule constants over five violation classes, every judgement made by comparing the document's claim to what `kompetenz.py` returns for the cited ID. `SKILL.md` Schritt 4 gates the turn on a zero exit code. **Two fail-open holes were found by orchestrator probing and fixed; both left the full suite green.** (a) A single running "current anchor" meant an application block appearing **before** the anchor was never validated — the same fabricated item passed or failed purely on block order; replaced by a two-pass resolution using the nearest preceding anchor, or every resolved anchor when none precedes. (b) The check keyed on `type == "list"`, so identical labelled items evaded it in any other block shape; it is now type-agnostic, keyed on a matching `label` plus an `items` list. Both have regressions. **The source-legitimate empty case is respected and pinned:** a bare correct anchor passes on all six shards including `PRIM.M` (`anwendungsbereiche_bindung: keine`) and `SEK1.E` (`prosa`); the checker only ever validates blocks that are present and never demands that one exist, so no shard can be pushed into inventing content to satisfy it. Unlabelled activity lists are never touched. Suite 1000 → 1019 passed, 3 skipped. |
| 2026-08-06 | E6-05: plan §6.6 requires `.pdf` ingestion "via text extraction"; `requirements-dev.txt` is exactly `jsonschema`, `pytest`, `python-docx` | A PDF text-extraction library would be added so `.pdf` teacher material converts like `.docx` | No PDF library is installed and none may be added without owner sign-off — the same delivery constraint that forced the E5-11 decision, where `python-docx` → `lxml` was accepted only because it was already upstream's runtime dependency. `.docx` conversion needs nothing new (it reuses `python-docx`). `.pdf` extraction has no zero-dependency path. | **DECIDED + DONE, orchestrator decision, no new dependency.** `docs_ingest._PDF_EXTRAKTOREN` is an ordered tuple of optional extractor callables (tries `pypdf`, then `pdfminer.six`); with neither installed, **every `.pdf` is logged as unusable and skipped, never fatal** — which is exactly the path §6.6 already prescribes for scanned/non-extractable PDFs, now simply reached for all PDFs. A future extractor is one appended function. Orchestrator-verified against a synthetic non-extractable PDF: it is reported in `Ingestionsbericht.uebersprungen`, its sibling `.md` is still accepted, and nothing raises. **Not verified with a real installed extractor** — impossible in this environment, and that is the point of the seam. Revisit only if the owner accepts a new runtime dependency. |
| 2026-08-06 | E6-05: `finde_lernaufgaben` return shape, and where the `docs/` contract is documented | The interim reader emitted `fach` as `"?.M"`, a placeholder for the unknown band; §6.7's ~2k-token reference budget suggested extending `references/kompetenzdaten.md` | A `docs/` folder never encodes a band — the convention is `docs/<fach>/<stufe>/…`, so the band is genuinely absent rather than unknown, and `"?.M"` was a placeholder for something that does not exist. Separately, `kompetenzdaten.md` measured **1,963 tokens** against the ~2k soft target: roughly 37 tokens of headroom, so any real addition breaches it. | **DECIDED + DONE.** `fach` now carries the bare subject code (`"M"`, `"D"`, `"E"`, `"SU"`) or `None` for unassigned material; orchestrator-verified that the E4-08 contract suite (`test_kompetenz_contract.py`) and `test_kompetenz_access.py` both still pass, so no pinned public-surface assertion depended on the placeholder. The §6.7 sharding review was held and resolved by **splitting finer** — §6.7's own prescribed remedy — into a new `references/lehrkraft_material.md` at **1,095 measured tokens**; `kompetenzdaten.md` is untouched at 1,963 and did not breach its target. **Known stale and deliberately deferred:** `kompetenzdaten.md` item 5 still calls `finde_lernaufgaben` the "Interimsleser" with PDF/DOCX/limits open. Correcting it costs more tokens than that file has, so the fix is a pointer to `lehrkraft_material.md` written at integration, not an expansion. |
| 2026-08-06 | E6-06: rendering `⟦ABB:…⟧` formula tokens carried by **application items**, not by the quoted competence | E2-18 shipped the token contract with "inline docx/HTML rendering", so any block supplying an `abbildungen` array was expected to resolve its tokens | **The mechanism is wired to exactly one block type.** Measured: `split_abbildungen` has one call site per renderer — inside the `kompetenzbezug` verbatim path (`render_lesson_docx.py:113`, `render_lesson_html.py:80`). `_emit_list` calls `add_md`, so a `list` block's `abbildungen` array is inert. Two of `SEK1.M.ZAHLEN.K2.02`'s **seven binding items** (`AB.ZAHLEN.K2.06`, `.K2.07`) carry image tokens; rendered, the `beobachtungsbogen` shows **2 literal `ABB:` strings and 0 embedded media**. Retyping the block as `kompetenzbezug` does not work either: `pruefe_verankerung.py` validates every such block against `kompetenz_nach_id`, which rejects an Anwendungsitem ID outright (`id-unaufloesbar … ist eine Anwendungsitem-ID (Art=AB)`), and substituting the owning competence ID then fails the verbatim text compare. No existing block type satisfies both the checker and the image path. | **Recorded, NOT fixed — new task E5-13.** E6-06 ships the two items with correct `abbildungen` metadata copied unchanged from the API, so the data is right and the fix is a pure renderer change. Deliberately **not** made inside E6-06: it touches vendored, byte-stable renderer sources in **both** skills, and `tests/test_renderer_golden.py` additionally pins the paired sources as byte-identical, so it is four coordinated files against a golden invariant — E5 work, not fixture work. **Shipping the literal token is the safe interim state, by the mechanism's own design:** `split_abbildungen`'s docstring keeps an unmatched token as literal text because "a stray token character is visible and obviously wrong, a vanished one is not". Nothing is fabricated and nothing is lost. **Superseded an orchestrator error in the review of this task:** the coordinator reported `K2.06` as containing no image token; that was an artifact of truncating the text at 110 characters in the probe, and the agent's correction was verified against the untruncated API result. The item does carry `⟦ABB:hauptdokument.img8is.png⟧`. |
| 2026-08-06 | E7-01/E7-02: plan §6.5's `at-differenzierung` frontmatter, which the task requires to ship **verbatim**, vs. the measured axis data (**V-61**) | The mandated `description` and the shipped axis metadata would agree | They do not. The frontmatter says *"Mathematik: Standard/Standard AHS + „allenfalls"-Inhalte; Sprachen: GERS A1/A2/B1"* — the pre-V-61 simplification from plan §4.7. Measured: `standard_standardplus` applies to **all three** Sek I subjects (SEK1.D, SEK1.M, SEK1.E) from `gilt_ab_stufe: K2`, `gers` is a **SEK1.E-only sub-axis** (not "the languages"), and `enrichment_quelle: "allenfalls"` exists on **SEK1.M alone** — orchestrator-verified across all 247 records: 21 of 42 SEK1.M records carry `enrichment_items`, **0** in each of the other five shards. | **DECIDED + DONE, resolved without editing the frontmatter.** The verbatim requirement wins for the YAML block — it is the skill's routing surface and E6-01 set the precedent of shipping plan §6.5 unchanged. The body of `SKILL.md` names the contradiction explicitly, labels the frontmatter as *Routing-Text für das Laden der Skill, keine Datenquelle*, and states the corrected V-61 facts as the runtime contract, with the operative rule that **only the returned `achse` value decides**. Frontmatter verified byte-identical to plan §6.5 by programmatic diff, not by eye. This keeps the discovery surface intact while ensuring no generated plan is graded or tiered off the stale simplification. |
| 2026-08-06 | E5-13: closing **V-87** — `⟦ABB:…⟧` tokens resolved only inside `kompetenzbezug` | A narrow fix teaching `_emit_list` to call `split_abbildungen` would close the measured defect (the `beobachtungsbogen`'s two binding items) | A list-only patch would have re-created the same latent gap in every other prose-bearing block. The real fault was that `split_abbildungen` decided *where* an image sits but had exactly one caller per renderer, so the position logic was shared while the *routing* was not. **Ten** call sites across the two renderers legitimately carry prose that can hold a token. | **DECIDED + DONE.** A new shared `render_with_abbildungen(text, abbildungen, text_fn, image_fn)` sits beside `split_abbildungen` in `lesson_common.py` and drives the split, dispatching each segment to a text or image callback; each renderer wraps it once (`add_md_abb` / `md_abb`), falling through to plain `add_md`/`md` when `abbildungen` is falsy. Routed through it: `paragraph`, `labeled`, `instructions`, `list`/`checklist`, `callout`, `cards`, `table`, `fill_table`, `source_card`, and both unknown-type fallbacks. Labels, headings, table headers and citation footers deliberately stay on the plain path — structural short fields, not quotable prose, and docx never ran markdown on headings, so extending it there would have broken the pairing symmetry. **Measured after:** the `beobachtungsbogen` docx has **0** literal `ABB:` and embeds `image1/image2.png` whose SHA-256 equal the shipped `hauptdokument.img8is.png`/`img9is.png` byte-for-byte; the HTML emits 2 `<img class="abb">` and its only remaining `⟦ABB:` is inside the inlined `theme.css` comment. **The golden invariant held with zero goldens regenerated** (`git status` on `tests/fixtures/renderer_goldens/` clean), and all four paired renderer sources are byte-identical across both skills by `sha256sum`. **Orchestrator mutation-probed for breadth, not just for the reported defect:** removing the `abbildungen` forwarding from the **`cards`** path alone fails exactly `test_block_type_with_abbildungen_embeds_images_not_literal_tokens[cards]` and nothing else — the parametrised test covers seven block types independently, so this is provably not a list-only patch wearing a general name. Suite 1054 → **1078 passed**, 3 skipped. |
| 2026-08-10 | E7-03 below-tier sourcing for SEK1.M | `BACKLOG.md` E7-03 reads *below* = "core conceptions + prior-year `Wiederholen und Festigen`", i.e. one source | The access layer deliberately exposes two: `finde_differenzierung().vorklasse_stuetzen` returns positional predecessor **competences**, while the official `Wiederholen und Festigen` **application item** is a binding item reached via `finde_anwendungsbereiche`. `kompetenz.py`'s docstring explicitly forbids substituting the raw item for the competence graph. | **DECIDED + DONE (V-89).** The *below* tier uses **both**, in two separately labelled blocks, and the shipped `references/mathematik.md` states which is which. The backlog wording is not wrong, only compressed; no code change. |
| 2026-08-10 | Inline formula images on SEK1.M binding items (E7-03) | The E7-03 subagent brief stated that for `AT.LP23.SEK1.M.ZAHLEN.K2.03` only `AB.ZAHLEN.K2.17` and `.20` carry `⟦ABB:…⟧` tokens | Measured: `AB.ZAHLEN.K2.13` also carries 3 tokens. Binding items carry **7** distinct tokens (`.13`→3, `.20`→4); the `allenfalls` item `.17` carries **3** more; **10 total**. The subagent re-measured, contradicted the brief, and was correct. | **DECIDED + DONE (V-89).** Fixture builds its `abbildungen` arrays from the live `abbildungen` field per item rather than any enumeration, and a test pins the measured count. Root cause of the bad brief: the orchestrator characterised item text from a `[:110]` truncated dump — the **second** instance of the truncated-output error the 2026-08-06 handover already recorded. |
| 2026-08-10 | PRIM.SU application-area attribution in tier material (E7-04) | Plan §4.7 treats Anwendungsbereiche as precisifications of a competence, as they are for SEK1.M (`bindung: kompetenz`) | PRIM.SU binds at `stufe`: the same 10 items `AB.SCH3.01`–`.10` are returned for every competence of that Schulstufe across all six Kompetenzbereiche, and the items carry `kompetenz_id: None` with no domain field. Presenting them as precisifications of one competence is a misattribution. | **DECIDED + DONE (V-90).** Tier material selects a labelled subset and states that the selection is a pedagogical choice from a stage-wide list, never an official one-to-one binding. Enforced by internal-consistency tests (selection == the set the material's own disclaimer names). **Known limit:** a coordinated rewrite of both passes, and is structurally uncatchable until per-item domain attribution exists in the data. |
| 2026-08-10 | Scope of the `niveau_spalte`/origin-separation task (E7-05) | `BACKLOG.md` E7-05 reads as renderer work — "`niveau_spalte` rendering; official content visually separated from `docs/` material" | Both emitters already shipped and were correct: measured on the rendered package, the plan document writes one single-row 3-column `w:tbl` with each tier's display text and its own `w:tcBorders` left accent (`E8C98A`/`5BB088`/`7A8CC4`), and the two origin branches write distinct labels, icons and accents (`7A8CC4` official vs `B2652E` teacher material) with `Quelle:` emitted only for `amtlich is True`. What was missing was a **`docs/`-sourced** block anywhere in a fixture — the only non-official block in the flow was skill-authored depth — and any test asserting the docx bytes rather than the fixture JSON. | **DECIDED + DONE.** No renderer change. The maths worked example gains one teacher-material section in `arbeitsblatt_unter`, traceable to the real ingestion fixture (`finde_lernaufgaben(fach="M", stufe="K2")` → `mathematik/K2/bruchrechnen.md`), and `tests/test_niveau_spalte_rendering.py` (9 tests) asserts tiers and origin separation against `word/document.xml`. Mutation-verified: dropping the accent, reversing tier order, forcing `resolve_herkunft` to the official branch, and falsifying the docs provenance each fail the corresponding tests. |
| 2026-08-10 | `herkunftsblock` label for skill-authored depth (E7-05) | The false branch of `resolve_herkunft` renders as `Lehrkraft-Material (nicht amtlich)`, which reads as "a file the teacher supplied" | The `über` worksheet uses that same branch for **skill-authored** depth, which no teacher wrote. The label is imprecise there; the flag it encodes (`amtlich: false`) is correct, and the safe direction is preserved — content that is not official is never shown as official. | **NOT CHANGED — recorded as a known limit.** `HERKUNFT_LABEL` lives in `lesson_common.py`, which must stay byte-identical across both skills and is pinned by four DOCX goldens; a wording change would force a golden regeneration, which is forbidden. Splitting the false branch into "teacher material" vs "skill-authored" is a renderer-contract decision for the owner, not a side effect of a fixture task. |
